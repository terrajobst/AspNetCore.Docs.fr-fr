---
uid: web-pages/overview/getting-started/introducing-razor-syntax-c
title: "Introduction à la programmation Web ASP.NET à l’aide de la syntaxe Razor (c#) | Documents Microsoft"
author: tfitzmac
description: "Ce chapitre vous donne une vue d’ensemble de la programmation avec les Pages Web ASP.NET à l’aide de la syntaxe Razor. ASP.NET est la technologie de Microsoft pour les pa web dynamique en cours d’exécution..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: aa67d304-583b-4bf8-a231-195656cfb587
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-c
msc.type: authoredcontent
ms.openlocfilehash: 32cdd8d524d783d7ccc3ab076de636ce4a868132
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 01/24/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-c"></a><span data-ttu-id="93a57-104">Introduction à la programmation Web ASP.NET à l’aide de la syntaxe Razor (c#)</span><span class="sxs-lookup"><span data-stu-id="93a57-104">Introduction to ASP.NET Web Programming Using the Razor Syntax (C#)</span></span>
====================
<span data-ttu-id="93a57-105">par [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="93a57-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="93a57-106">Cet article vous donne une vue d’ensemble de la programmation avec les Pages Web ASP.NET à l’aide de la syntaxe Razor.</span><span class="sxs-lookup"><span data-stu-id="93a57-106">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax.</span></span> <span data-ttu-id="93a57-107">ASP.NET est la technologie de Microsoft pour les pages web dynamiques en cours d’exécution sur les serveurs web.</span><span class="sxs-lookup"><span data-stu-id="93a57-107">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span> <span data-ttu-id="93a57-108">Cette traite des articles sur l’utilisation du langage de programmation c#.</span><span class="sxs-lookup"><span data-stu-id="93a57-108">This articles focuses on using the C# programming language.</span></span>
> 
> <span data-ttu-id="93a57-109">**Vous apprendrez**:</span><span class="sxs-lookup"><span data-stu-id="93a57-109">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="93a57-110">Les 8 supérieur programmation des conseils de mise en route avec la programmation à l’aide de la syntaxe Razor ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="93a57-110">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="93a57-111">Concepts de programmation de base que vous aurez besoin.</span><span class="sxs-lookup"><span data-stu-id="93a57-111">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="93a57-112">Le code de serveur ASP.NET et la syntaxe Razor concerne tous.</span><span class="sxs-lookup"><span data-stu-id="93a57-112">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="93a57-113">Versions du logiciel</span><span class="sxs-lookup"><span data-stu-id="93a57-113">Software versions</span></span>
> 
> 
> - <span data-ttu-id="93a57-114">Pages Web ASP.NET (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="93a57-114">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="93a57-115">Ce didacticiel fonctionne également avec ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="93a57-115">This tutorial also works with ASP.NET Web Pages 2.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="93a57-116">Les conseils de programmation 8 supérieur</span><span class="sxs-lookup"><span data-stu-id="93a57-116">The Top 8 Programming Tips</span></span>

<span data-ttu-id="93a57-117">Cette section répertorie quelques conseils qui vous avez absolument besoin de savoir que vous commencez à écrire le code de serveur ASP.NET à l’aide de la syntaxe Razor.</span><span class="sxs-lookup"><span data-stu-id="93a57-117">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

> [!NOTE]
> <span data-ttu-id="93a57-118">La syntaxe Razor est basée sur le langage de programmation c#, et qui est la langue qui est utilisée plus souvent avec les Pages Web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="93a57-118">The Razor syntax is based on the C# programming language, and that's the language that's used most often with ASP.NET Web Pages.</span></span> <span data-ttu-id="93a57-119">Toutefois, la syntaxe Razor prend également en charge du langage Visual Basic et tout ce que vous voyez que vous pouvez également faire dans Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="93a57-119">However, the Razor syntax also supports the Visual Basic language, and everything you see you can also do in Visual Basic.</span></span> <span data-ttu-id="93a57-120">Pour plus d’informations, consultez l’annexe [langage Visual Basic et la syntaxe](https://go.microsoft.com/fwlink/?LinkId=202908).</span><span class="sxs-lookup"><span data-stu-id="93a57-120">For details, see the appendix [Visual Basic Language and Syntax](https://go.microsoft.com/fwlink/?LinkId=202908).</span></span>


<span data-ttu-id="93a57-121">Vous trouverez plus de détails sur la plupart de ces techniques de programmation plus loin dans l’article.</span><span class="sxs-lookup"><span data-stu-id="93a57-121">You can find more details about most of these programming techniques later in the article.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="93a57-122">1. Ajouter du code à une page à l’aide du caractère « @ »</span><span class="sxs-lookup"><span data-stu-id="93a57-122">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="93a57-123">Le `@` caractère démarre les expressions inline, les blocs d’instruction unique et les blocs d’instructions multiples :</span><span class="sxs-lookup"><span data-stu-id="93a57-123">The `@` character starts inline expressions, single statement blocks, and multi-statement blocks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample1.html)]

<span data-ttu-id="93a57-124">Il s’agit d’aspect de ces instructions lorsque la page s’exécute dans un navigateur :</span><span class="sxs-lookup"><span data-stu-id="93a57-124">This is what these statements look like when the page runs in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-c/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="93a57-126">**Codage HTML**</span><span class="sxs-lookup"><span data-stu-id="93a57-126">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="93a57-127">Lorsque vous affichez le contenu dans une page à l’aide de la `@` de caractères, comme dans les exemples précédents, ASP.NET encode au format HTML la sortie.</span><span class="sxs-lookup"><span data-stu-id="93a57-127">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="93a57-128">Cette opération remplace les caractères réservés HTML (tel que `<` et `>` et `&`) avec les codes qui permettent les caractères à afficher en tant que caractères dans une page web au lieu d’être interprétée comme des balises HTML ou les entités.</span><span class="sxs-lookup"><span data-stu-id="93a57-128">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="93a57-129">Sans codage HTML, la sortie à partir de votre code serveur peuvent ne pas s’afficher correctement et peut exposer les risques de sécurité d’une page.</span><span class="sxs-lookup"><span data-stu-id="93a57-129">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="93a57-130">Si votre objectif est de sortie de balisage HTML qui rend les balises en tant que balisage (par exemple `<p></p>` d’un paragraphe ou `<em></em>` pour mettre en évidence le texte), consultez la section [combinaison de texte, le balisage et le Code dans les blocs de Code](#BM_CombiningTextMarkupAndCode) plus loin dans cet article.</span><span class="sxs-lookup"><span data-stu-id="93a57-130">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="93a57-131">Vous pouvez en savoir plus sur le codage HTML dans [utilisation des formulaires](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="93a57-131">You can read more about HTML encoding in [Working with Forms](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-in-braces"></a><span data-ttu-id="93a57-132">2. Vous placez des blocs de code entre accolades</span><span class="sxs-lookup"><span data-stu-id="93a57-132">2. You enclose code blocks in braces</span></span>

<span data-ttu-id="93a57-133">A *bloc de code* inclut une ou plusieurs instructions de code et est placé entre accolades.</span><span class="sxs-lookup"><span data-stu-id="93a57-133">A *code block* includes one or more code statements and is enclosed in braces.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample2.html)]

<span data-ttu-id="93a57-134">Le résultat est affiché dans un navigateur :</span><span class="sxs-lookup"><span data-stu-id="93a57-134">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-c/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-semicolon"></a><span data-ttu-id="93a57-136">3. À l’intérieur d’un bloc, vous mettez fin à chaque instruction du code par un point-virgule</span><span class="sxs-lookup"><span data-stu-id="93a57-136">3. Inside a block, you end each code statement with a semicolon</span></span>

<span data-ttu-id="93a57-137">À l’intérieur d’un bloc de code, chaque instruction de code complet doit se terminer par un point-virgule.</span><span class="sxs-lookup"><span data-stu-id="93a57-137">Inside a code block, each complete code statement must end with a semicolon.</span></span> <span data-ttu-id="93a57-138">Expressions inline ne pas se terminer par un point-virgule.</span><span class="sxs-lookup"><span data-stu-id="93a57-138">Inline expressions don't end with a semicolon.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample3.html)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="93a57-139">4. Vous permet de stocker les valeurs de variables</span><span class="sxs-lookup"><span data-stu-id="93a57-139">4. You use variables to store values</span></span>

<span data-ttu-id="93a57-140">Vous pouvez stocker des valeurs dans une *variable*, y compris les chaînes, nombres et dates, etc. Vous créez une variable en utilisant la `var` (mot clé).</span><span class="sxs-lookup"><span data-stu-id="93a57-140">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `var` keyword.</span></span> <span data-ttu-id="93a57-141">Vous pouvez insérer des valeurs de variables directement dans une page à l’aide de `@`.</span><span class="sxs-lookup"><span data-stu-id="93a57-141">You can insert variable values directly in a page using `@`.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample4.html)]

<span data-ttu-id="93a57-142">Le résultat est affiché dans un navigateur :</span><span class="sxs-lookup"><span data-stu-id="93a57-142">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-c/_static/image3.jpg)

<a id="ID_StringLiterals"></a>
### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="93a57-144">5. Vous placez les valeurs de chaîne littérale entre des guillemets doubles</span><span class="sxs-lookup"><span data-stu-id="93a57-144">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="93a57-145">A *chaîne* est une séquence de caractères qui sont traitées en tant que texte.</span><span class="sxs-lookup"><span data-stu-id="93a57-145">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="93a57-146">Pour spécifier une chaîne, placez-le entre guillemets doubles :</span><span class="sxs-lookup"><span data-stu-id="93a57-146">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample5.cshtml)]

<span data-ttu-id="93a57-147">Si la chaîne que vous souhaitez afficher contient une barre oblique inverse ( `\` ) ou des guillemets doubles ( `"` ), utilisez un *littéral de chaîne textuel* qui est préfixé avec le `@` opérateur.</span><span class="sxs-lookup"><span data-stu-id="93a57-147">If the string that you want to display contains a backslash character ( `\` ) or double quotation marks ( `"` ), use a *verbatim string literal* that's prefixed with the `@` operator.</span></span> <span data-ttu-id="93a57-148">(En c#, le \ caractère a une signification spéciale, sauf si vous utilisez un littéral de chaîne textuel.)</span><span class="sxs-lookup"><span data-stu-id="93a57-148">(In C#, the \ character has special meaning unless you use a verbatim string literal.)</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample6.html)]

<span data-ttu-id="93a57-149">Pour incorporer des guillemets doubles, utilisez un littéral de chaîne textuel et répétez les guillemets :</span><span class="sxs-lookup"><span data-stu-id="93a57-149">To embed double quotation marks, use a verbatim string literal and repeat the quotation marks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample7.html)]

<span data-ttu-id="93a57-150">Voici le résultat de l’utilisation de ces deux exemples dans une page :</span><span class="sxs-lookup"><span data-stu-id="93a57-150">Here's the result of using both of these examples in a page:</span></span>

![Razor-Img4](introducing-razor-syntax-c/_static/image4.jpg)

> [!NOTE]
> <span data-ttu-id="93a57-152">Notez que le `@` caractère est utilisé pour marquer des littéraux de chaîne textuelle en c# et marquer du code dans les pages ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="93a57-152">Notice that the `@` character is used both to mark verbatim string literals in C# and to mark code in ASP.NET pages.</span></span>


### <a name="6-code-is-case-sensitive"></a><span data-ttu-id="93a57-153">6. Code respecte la casse</span><span class="sxs-lookup"><span data-stu-id="93a57-153">6. Code is case sensitive</span></span>

<span data-ttu-id="93a57-154">En c#, mots clés (comme `var`, `true`, et `if`) et les noms de variables respectent la casse.</span><span class="sxs-lookup"><span data-stu-id="93a57-154">In C#, keywords (like `var`, `true`, and `if`) and variable names are case sensitive.</span></span> <span data-ttu-id="93a57-155">Les lignes de code suivantes créent deux variables différentes, `lastName` et`LastName.`</span><span class="sxs-lookup"><span data-stu-id="93a57-155">The following lines of code create two different variables, `lastName` and `LastName.`</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample8.cshtml)]

<span data-ttu-id="93a57-156">Si vous déclarez une variable en tant que `var lastName = "Smith";` et si vous tentez de référencer cette variable dans la page `@LastName`, une erreur se produit, car `LastName` ne sera pas reconnue.</span><span class="sxs-lookup"><span data-stu-id="93a57-156">If you declare a variable as `var lastName = "Smith";` and if you try to reference that variable in your page as `@LastName`, an error results because `LastName` won't be recognized.</span></span>

> [!NOTE]
> <span data-ttu-id="93a57-157">En Visual Basic, mots clés et les variables sont *pas* respecte la casse.</span><span class="sxs-lookup"><span data-stu-id="93a57-157">In Visual Basic, keywords and variables are *not* case sensitive.</span></span>


### <a name="7-much-of-your-coding-involves-objects"></a><span data-ttu-id="93a57-158">7. Une grande partie de votre programmation implique des objets</span><span class="sxs-lookup"><span data-stu-id="93a57-158">7. Much of your coding involves objects</span></span>

<span data-ttu-id="93a57-159">Un *objet* représente une chose que vous pouvez programmer avec &#8212; une page, une zone de texte, un fichier, une image, une requête web, un message électronique, un enregistrement de client (ligne de base de données), etc. Objets ont des propriétés qui décrivent leurs caractéristiques et que vous pouvez lire ou Modifier &#8212; un objet de zone de texte a un `Text` propriété (entre autres), un objet de requête a un `Url` propriété, un message électronique a un `From` propriété et un objet customer possède une `FirstName` propriété.</span><span class="sxs-lookup"><span data-stu-id="93a57-159">An *object* represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics and that you can read or change &#8212; a text box object has a `Text` property (among others), a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="93a57-160">Objets ont également des méthodes qui sont le &quot;verbes&quot; qu’ils peuvent effectuer.</span><span class="sxs-lookup"><span data-stu-id="93a57-160">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="93a57-161">Exemples d’un objet fichier `Save` méthode, d’un objet image `Rotate` (méthode) et un objet de messagerie `Send` (méthode).</span><span class="sxs-lookup"><span data-stu-id="93a57-161">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="93a57-162">Vous travaillerez souvent le `Request` de l’objet, qui fournit des informations telles que les valeurs des zones de texte (champs de formulaire) dans la page, le type de navigateur fait la demande, l’URL de la page, de l’identité de l’utilisateur, etc. L’exemple suivant montre comment accéder aux propriétés de la `Request` objet et comment appeler le `MapPath` méthode de la `Request` objet, ce qui vous donne le chemin d’accès absolu de la page sur le serveur :</span><span class="sxs-lookup"><span data-stu-id="93a57-162">You'll often work with the `Request` object, which gives you information like the values of text boxes (form fields) on the page, what type of browser made the request, the URL of the page, the user identity, etc. The following example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample9.html)]

<span data-ttu-id="93a57-163">Le résultat est affiché dans un navigateur :</span><span class="sxs-lookup"><span data-stu-id="93a57-163">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-c/_static/image5.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="93a57-165">8. Vous pouvez écrire du code qui prend des décisions</span><span class="sxs-lookup"><span data-stu-id="93a57-165">8. You can write code that makes decisions</span></span>

<span data-ttu-id="93a57-166">Une fonctionnalité clé de pages web dynamiques est que vous pouvez déterminer quoi faire en fonction des conditions.</span><span class="sxs-lookup"><span data-stu-id="93a57-166">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="93a57-167">La méthode la plus courante pour cela est avec le `if` instruction (et facultatifs `else` instruction).</span><span class="sxs-lookup"><span data-stu-id="93a57-167">The most common way to do this is with the `if` statement (and optional `else` statement).</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample10.cshtml)]

<span data-ttu-id="93a57-168">L’instruction `if(IsPost)` est un moyen rapide de l’écriture de `if(IsPost == true)`.</span><span class="sxs-lookup"><span data-stu-id="93a57-168">The statement `if(IsPost)` is a shorthand way of writing `if(IsPost == true)`.</span></span> <span data-ttu-id="93a57-169">Avec `if` instructions, il existe plusieurs façons de tester des conditions, la répétition des blocs de code, et ainsi de suite, qui sont décrit plus loin dans cet article.</span><span class="sxs-lookup"><span data-stu-id="93a57-169">Along with `if` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="93a57-170">Le résultat affiché dans un navigateur (après avoir cliqué sur **Submit**) :</span><span class="sxs-lookup"><span data-stu-id="93a57-170">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor-Img6](introducing-razor-syntax-c/_static/image6.jpg)

> [!TIP] 
> 
> <a id="SB_HttpGetPost"></a>
> ### <a name="http-get-and-post-methods-and-the-ispost-property"></a><span data-ttu-id="93a57-172">HTTP GET et POST méthodes et la propriété IsPost</span><span class="sxs-lookup"><span data-stu-id="93a57-172">HTTP GET and POST Methods and the IsPost Property</span></span>
> 
> <span data-ttu-id="93a57-173">Le protocole utilisé pour les pages web (HTTP) prend en charge un nombre très limité de méthodes (verbes) qui sont utilisés pour effectuer des demandes au serveur.</span><span class="sxs-lookup"><span data-stu-id="93a57-173">The protocol used for web pages (HTTP) supports a very limited number of methods (verbs) that are used to make requests to the server.</span></span> <span data-ttu-id="93a57-174">Les deux plus courantes sont GET, qui est utilisé pour lire une page, et POST, qui est utilisée pour envoyer une page.</span><span class="sxs-lookup"><span data-stu-id="93a57-174">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="93a57-175">En général, la première fois qu’un utilisateur demande une page, la page est demandée à l’aide de GET.</span><span class="sxs-lookup"><span data-stu-id="93a57-175">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="93a57-176">Si l’utilisateur remplit un formulaire, puis clique sur un bouton d’envoi, le navigateur envoie une demande POST sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="93a57-176">If the user fills in a form and then clicks a submit button, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="93a57-177">Dans la programmation web, il est souvent utile de savoir si une page est demandée sous la forme d’une opération GET ou une publication afin que vous sachiez comment traiter la page.</span><span class="sxs-lookup"><span data-stu-id="93a57-177">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="93a57-178">Dans ASP.NET Web Pages, vous pouvez utiliser le `IsPost` propriété pour voir si une requête est une opération GET ou POST.</span><span class="sxs-lookup"><span data-stu-id="93a57-178">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="93a57-179">Si la demande est une publication, le `IsPost` propriété retournera la valeur true, et vous pouvez effectuer les opérations en lecture les valeurs des zones de texte sur un formulaire.</span><span class="sxs-lookup"><span data-stu-id="93a57-179">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="93a57-180">Vous voyez de nombreux exemples vous montrent comment traiter la page différemment selon la valeur de `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="93a57-180">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="93a57-181">Un exemple de Code Simple</span><span class="sxs-lookup"><span data-stu-id="93a57-181">A Simple Code Example</span></span>

<span data-ttu-id="93a57-182">Cette procédure vous montre comment créer une page qui illustre des techniques de programmation de base.</span><span class="sxs-lookup"><span data-stu-id="93a57-182">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="93a57-183">Dans l’exemple, vous créez une page qui permet aux utilisateurs d’entrer les deux numéros, puis il ajoute les et affiche le résultat.</span><span class="sxs-lookup"><span data-stu-id="93a57-183">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="93a57-184">Dans votre éditeur, créez un nouveau fichier et nommez-la *AddNumbers.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="93a57-184">In your editor, create a new file and name it *AddNumbers.cshtml*.</span></span>
2. <span data-ttu-id="93a57-185">Copiez le code et le balisage suivant dans la page, en remplaçant tout élément déjà dans la page.</span><span class="sxs-lookup"><span data-stu-id="93a57-185">Copy the following code and markup into the page, replacing anything already in the page.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample11.cshtml)]

    <span data-ttu-id="93a57-186">Voici quelques points de noter :</span><span class="sxs-lookup"><span data-stu-id="93a57-186">Here are some things for you to note:</span></span>

    - <span data-ttu-id="93a57-187">Le `@` caractère démarre le premier bloc de code dans la page, et il précède le `totalMessage` variable qui est incorporé au bas de la page.</span><span class="sxs-lookup"><span data-stu-id="93a57-187">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable that's embedded near the bottom of the page.</span></span>
    - <span data-ttu-id="93a57-188">Le bloc en haut de la page est placé entre accolades.</span><span class="sxs-lookup"><span data-stu-id="93a57-188">The block at the top of the page is enclosed in braces.</span></span>
    - <span data-ttu-id="93a57-189">Dans le bloc en haut, toutes les lignes se terminer par un point-virgule.</span><span class="sxs-lookup"><span data-stu-id="93a57-189">In the block at the top, all lines end with a semicolon.</span></span>
    - <span data-ttu-id="93a57-190">Les variables `total`, `num1`, `num2`, et `totalMessage` stocker plusieurs nombres et une chaîne.</span><span class="sxs-lookup"><span data-stu-id="93a57-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="93a57-191">La valeur de chaîne littérale assignée à la `totalMessage` variable est entre guillemets doubles.</span><span class="sxs-lookup"><span data-stu-id="93a57-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="93a57-192">Étant donné que le code respecte la casse, lorsque le `totalMessage` variable est utilisée au bas de la page, son nom doit correspondre exactement à la variable en haut.</span><span class="sxs-lookup"><span data-stu-id="93a57-192">Because the code is case-sensitive, when the `totalMessage` variable is used near the bottom of the page, its name must match the variable at the top exactly.</span></span>
    - <span data-ttu-id="93a57-193">L’expression `num1.AsInt() + num2.AsInt()` montre comment utiliser des objets et méthodes.</span><span class="sxs-lookup"><span data-stu-id="93a57-193">The expression `num1.AsInt() + num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="93a57-194">Le `AsInt` méthode sur chaque variable convertit la chaîne entrée par un utilisateur à un nombre (entier) afin que vous pouvez effectuer une opération arithmétique sur celui-ci.</span><span class="sxs-lookup"><span data-stu-id="93a57-194">The `AsInt` method on each variable converts the string entered by a user to a number (an integer) so that you can perform arithmetic on it.</span></span>
    - <span data-ttu-id="93a57-195">Le `<form>` balise inclut un `method="post"` attribut.</span><span class="sxs-lookup"><span data-stu-id="93a57-195">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="93a57-196">Spécifie que lorsque l’utilisateur clique sur **ajouter**, la page sera être envoyée au serveur à l’aide de la méthode HTTP POST.</span><span class="sxs-lookup"><span data-stu-id="93a57-196">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="93a57-197">Lorsque la page est envoyée, la `if(IsPost)` test prend la valeur true et que la condition de code s’exécute, affiche le résultat de l’ajout de nombres.</span><span class="sxs-lookup"><span data-stu-id="93a57-197">When the page is submitted, the `if(IsPost)` test evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="93a57-198">Enregistrez la page et l’exécuter dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="93a57-198">Save the page and run it in a browser.</span></span> <span data-ttu-id="93a57-199">(Assurez-vous que la page est sélectionnée dans le **fichiers** espace de travail avant de l’exécuter.) Entrez deux nombres entiers, puis activez la **ajouter** bouton.</span><span class="sxs-lookup"><span data-stu-id="93a57-199">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span> 

    ![Razor-Img7](introducing-razor-syntax-c/_static/image7.jpg)

## <a name="basic-programming-concepts"></a><span data-ttu-id="93a57-201">Concepts de programmation de base</span><span class="sxs-lookup"><span data-stu-id="93a57-201">Basic Programming Concepts</span></span>

<span data-ttu-id="93a57-202">Cet article vous offre une vue d’ensemble de la programmation web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="93a57-202">This article provides you with an overview of ASP.NET web programming.</span></span> <span data-ttu-id="93a57-203">Il n’est pas un examen exhaustif, simplement une visite rapide via les concepts de programmation que vous utiliserez le plus souvent.</span><span class="sxs-lookup"><span data-stu-id="93a57-203">It isn't an exhaustive examination, just a quick tour through the programming concepts you'll use most often.</span></span> <span data-ttu-id="93a57-204">Même dans ce cas, elle couvre presque tout ce dont vous aurez besoin pour démarrer avec ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="93a57-204">Even so, it covers almost everything you'll need to get started with ASP.NET Web Pages.</span></span>

<span data-ttu-id="93a57-205">Mais tout d’abord, un arrière-plan de technique peu.</span><span class="sxs-lookup"><span data-stu-id="93a57-205">But first, a little technical background.</span></span>

### <a name="the-razor-syntax-server-code-and-aspnet"></a><span data-ttu-id="93a57-206">La syntaxe Razor, ASP.NET et Code serveur</span><span class="sxs-lookup"><span data-stu-id="93a57-206">The Razor Syntax, Server Code, and ASP.NET</span></span>

<span data-ttu-id="93a57-207">La syntaxe Razor est une syntaxe de programmation simple pour l’incorporation de code serveur dans une page web.</span><span class="sxs-lookup"><span data-stu-id="93a57-207">Razor syntax is a simple programming syntax for embedding server-based code in a web page.</span></span> <span data-ttu-id="93a57-208">Dans une page web qui utilise la syntaxe Razor, il existe deux types de contenu : le code client contenu et le serveur.</span><span class="sxs-lookup"><span data-stu-id="93a57-208">In a web page that uses the Razor syntax, there are two kinds of content: client content and server code.</span></span> <span data-ttu-id="93a57-209">Contenu du client est les sélections que vous êtes habitué à dans les pages web : le balisage HTML (éléments), des informations de style comme CSS, peut-être un script client tels que JavaScript et texte brut.</span><span class="sxs-lookup"><span data-stu-id="93a57-209">Client content is the stuff you're used to in web pages: HTML markup (elements), style information such as CSS, maybe some client script such as JavaScript, and plain text.</span></span>

<span data-ttu-id="93a57-210">La syntaxe Razor vous permet d’ajouter le code serveur à ce contenu client.</span><span class="sxs-lookup"><span data-stu-id="93a57-210">Razor syntax lets you add server code to this client content.</span></span> <span data-ttu-id="93a57-211">S’il existe le code serveur dans la page, le serveur exécute ce code en premier lieu, avant d’envoyer la page au navigateur.</span><span class="sxs-lookup"><span data-stu-id="93a57-211">If there's server code in the page, the server runs that code first, before it sends the page to the browser.</span></span> <span data-ttu-id="93a57-212">S’exécute sur le serveur, le code peut effectuer des tâches qui peuvent être beaucoup plus complexes à l’aide du contenu du client seul, comme l’accès à des bases de données du serveur.</span><span class="sxs-lookup"><span data-stu-id="93a57-212">By running on the server, the code can perform tasks that can be a lot more complex to do using client content alone, like accessing server-based databases.</span></span> <span data-ttu-id="93a57-213">Plus important encore, code serveur peut créer dynamiquement le client contenu &#8212; Il peut générer le balisage HTML ou tout autre contenu à la volée et puis l’envoyer au navigateur, ainsi que toutes les données HTML statique contenant la page.</span><span class="sxs-lookup"><span data-stu-id="93a57-213">Most importantly, server code can dynamically create client content &#8212; it can generate HTML markup or other content on the fly and then send it to the browser along with any static HTML that the page might contain.</span></span> <span data-ttu-id="93a57-214">Du point de vue du navigateur, client à du contenu qui est généré par votre code serveur n’est pas différente de tout autre contenu du client.</span><span class="sxs-lookup"><span data-stu-id="93a57-214">From the browser's perspective, client content that's generated by your server code is no different than any other client content.</span></span> <span data-ttu-id="93a57-215">Comme vous l’avez déjà vu, le code de serveur nécessaire est très simple.</span><span class="sxs-lookup"><span data-stu-id="93a57-215">As you've already seen, the server code that's required is quite simple.</span></span>

<span data-ttu-id="93a57-216">Les pages web ASP.NET qui incluent la syntaxe Razor ont une extension de fichier spécial (*.cshtml* ou *.vbhtml*).</span><span class="sxs-lookup"><span data-stu-id="93a57-216">ASP.NET web pages that include the Razor syntax have a special file extension (*.cshtml* or *.vbhtml*).</span></span> <span data-ttu-id="93a57-217">Le serveur reconnaît ces extensions, exécute le code qui est marqué avec la syntaxe Razor, puis envoie la page au navigateur.</span><span class="sxs-lookup"><span data-stu-id="93a57-217">The server recognizes these extensions, runs the code that's marked with Razor syntax, and then sends the page to the browser.</span></span>

### <a name="where-does-aspnet-fit-in"></a><span data-ttu-id="93a57-218">Quand ASP.NET se convient-elle ?</span><span class="sxs-lookup"><span data-stu-id="93a57-218">Where does ASP.NET fit in?</span></span>

<span data-ttu-id="93a57-219">La syntaxe Razor est basée sur une technologie de Microsoft, appelée ASP.NET, qui à son tour est basé sur Microsoft .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="93a57-219">Razor syntax is based on a technology from Microsoft called ASP.NET, which in turn is based on the Microsoft .NET Framework.</span></span> <span data-ttu-id="93a57-220">Le.NET Framework est une infrastructure de programmation big, complète de Microsoft pour le développement de pratiquement n’importe quel type d’application de l’ordinateur.</span><span class="sxs-lookup"><span data-stu-id="93a57-220">The.NET Framework is a big, comprehensive programming framework from Microsoft for developing virtually any type of computer application.</span></span> <span data-ttu-id="93a57-221">ASP.NET est la partie du .NET Framework qui est conçu spécialement pour la création d’applications web.</span><span class="sxs-lookup"><span data-stu-id="93a57-221">ASP.NET is the part of the .NET Framework that's specifically designed for creating web applications.</span></span> <span data-ttu-id="93a57-222">Les développeurs ont utilisé ASP.NET pour la création des plus grande et la plus élevée du trafic des sites Web dans le monde.</span><span class="sxs-lookup"><span data-stu-id="93a57-222">Developers have used ASP.NET to create many of the largest and highest-traffic websites in the world.</span></span> <span data-ttu-id="93a57-223">(Chaque fois que vous voyez l’extension de nom de fichier *.aspx* dans le cadre de l’URL d’un site, vous savez que le site a été écrite à l’aide d’ASP.NET.)</span><span class="sxs-lookup"><span data-stu-id="93a57-223">(Any time you see the file-name extension *.aspx* as part of the URL in a site, you'll know that the site was written using ASP.NET.)</span></span>

<span data-ttu-id="93a57-224">La syntaxe Razor offre toute la puissance d’ASP.NET, mais à l’aide d’une syntaxe simplifiée est plus facile de savoir si vous êtes débutant et qui vous rend plus productif si vous êtes un utilisateur expérimenté.</span><span class="sxs-lookup"><span data-stu-id="93a57-224">The Razor syntax gives you all the power of ASP.NET, but using a simplified syntax that's easier to learn if you're a beginner and that makes you more productive if you're an expert.</span></span> <span data-ttu-id="93a57-225">Bien que cette syntaxe est simple à utiliser, sa relation famille avec ASP.NET et le .NET Framework signifie que que vos sites Web est sophistiquées, plus vous disposez de la puissance des infrastructures de plus grandes à votre disposition.</span><span class="sxs-lookup"><span data-stu-id="93a57-225">Even though this syntax is simple to use, its family relationship to ASP.NET and the .NET Framework means that as your websites become more sophisticated, you have the power of the larger frameworks available to you.</span></span>

![Razor-Img8](introducing-razor-syntax-c/_static/image8.jpg)

> [!TIP] 
> 
> <span data-ttu-id="93a57-227">**Classes et Instances**</span><span class="sxs-lookup"><span data-stu-id="93a57-227">**Classes and Instances**</span></span>
> 
> <span data-ttu-id="93a57-228">Code de serveur ASP.NET utilise des objets, qui à son tour reposent sur le principe de classes.</span><span class="sxs-lookup"><span data-stu-id="93a57-228">ASP.NET server code uses objects, which are in turn built on the idea of classes.</span></span> <span data-ttu-id="93a57-229">La classe est la définition ou un modèle pour un objet.</span><span class="sxs-lookup"><span data-stu-id="93a57-229">The class is the definition or template for an object.</span></span> <span data-ttu-id="93a57-230">Par exemple, une application peut contenir un `Customer` classe qui définit les propriétés et méthodes qui a besoin d’un objet client.</span><span class="sxs-lookup"><span data-stu-id="93a57-230">For example, an application might contain a `Customer` class that defines the properties and methods that any customer object needs.</span></span>
> 
> <span data-ttu-id="93a57-231">Lorsque l’application a besoin travailler avec les informations client réel, il crée une instance de (ou *instancie*) un objet customer.</span><span class="sxs-lookup"><span data-stu-id="93a57-231">When the application needs to work with actual customer information, it creates an instance of (or *instantiates*) a customer object.</span></span> <span data-ttu-id="93a57-232">Chaque client est une instance distincte de la `Customer` classe.</span><span class="sxs-lookup"><span data-stu-id="93a57-232">Each individual customer is a separate instance of the `Customer` class.</span></span> <span data-ttu-id="93a57-233">Chaque instance prend en charge les mêmes propriétés et méthodes, mais les valeurs de propriété pour chaque instance sont en général différentes, car chaque objet customer est unique.</span><span class="sxs-lookup"><span data-stu-id="93a57-233">Every instance supports the same properties and methods, but the property values for each instance are typically different, because each customer object is unique.</span></span> <span data-ttu-id="93a57-234">Dans l’objet d’un client, le `LastName` propriété peut être « Smith » ; dans un autre objet client, le `LastName` propriété peut être « Jones ».</span><span class="sxs-lookup"><span data-stu-id="93a57-234">In one customer object, the `LastName` property might be "Smith"; in another customer object, the `LastName` property might be "Jones."</span></span>
> 
> <span data-ttu-id="93a57-235">De même, n’importe quelle page web individuelles dans votre site est un `Page` objet qui est une instance de la `Page` classe.</span><span class="sxs-lookup"><span data-stu-id="93a57-235">Similarly, any individual web page in your site is a `Page` object that's an instance of the `Page` class.</span></span> <span data-ttu-id="93a57-236">Un bouton dans la page est une `Button` objet qui est une instance de la `Button` classe et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="93a57-236">A button on the page is a `Button` object that is an instance of the `Button` class, and so on.</span></span> <span data-ttu-id="93a57-237">Chaque instance possède ses propres caractéristiques, mais ils sont basés sur ce qui est spécifié dans la définition de classe de l’objet.</span><span class="sxs-lookup"><span data-stu-id="93a57-237">Each instance has its own characteristics, but they all are based on what's specified in the object's class definition.</span></span>


## <a name="basic-syntax"></a><span data-ttu-id="93a57-238">Syntaxe de base</span><span class="sxs-lookup"><span data-stu-id="93a57-238">Basic Syntax</span></span>

<span data-ttu-id="93a57-239">Vous avez vu précédemment un exemple de base de la création d’une page ASP.NET Web Pages, et comment vous pouvez ajouter du code de serveur à la balise HTML.</span><span class="sxs-lookup"><span data-stu-id="93a57-239">Earlier you saw a basic example of how to create an ASP.NET Web Pages page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="93a57-240">Ici, vous allez apprendre les principes fondamentaux de l’écriture de code de serveur ASP.NET à l’aide de la syntaxe Razor &#8212; Autrement dit, les règles de langage programmation.</span><span class="sxs-lookup"><span data-stu-id="93a57-240">Here you'll learn the basics of writing ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="93a57-241">Si vous êtes familiarisé avec la programmation (surtout si vous avez utilisé C, C++, c#, Visual Basic ou JavaScript), une grande partie de ce que vous lisez ici sera facile.</span><span class="sxs-lookup"><span data-stu-id="93a57-241">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="93a57-242">Vous devrez probablement vous familiariser uniquement avec le mode code serveur est ajouté au balisage dans *.cshtml* fichiers.</span><span class="sxs-lookup"><span data-stu-id="93a57-242">You'll probably need to familiarize yourself only with how server code is added to markup in *.cshtml* files.</span></span>

<a id="BM_CombiningTextMarkupAndCode"></a>
### <a name="combining-text-markup-and-code-in-code-blocks"></a><span data-ttu-id="93a57-243">Combinaison de texte, le balisage et le Code dans les blocs de Code</span><span class="sxs-lookup"><span data-stu-id="93a57-243">Combining Text, Markup, and Code in Code Blocks</span></span>

<span data-ttu-id="93a57-244">Dans les blocs de code serveur, vous souhaitez souvent sortie texte ou balisage (ou les deux) à la page.</span><span class="sxs-lookup"><span data-stu-id="93a57-244">In server code blocks, you often want to output text or markup (or both) to the page.</span></span> <span data-ttu-id="93a57-245">Si un bloc de code serveur contient du texte qui ne s’est pas de code et qu’au lieu de cela doit être restituée en l’état, ASP.NET doit être capable de distinguer ce texte à partir du code.</span><span class="sxs-lookup"><span data-stu-id="93a57-245">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="93a57-246">Pour ce faire, plusieurs méthodes sont possibles.</span><span class="sxs-lookup"><span data-stu-id="93a57-246">There are several ways to do this.</span></span>

- <span data-ttu-id="93a57-247">Placez le texte dans un élément HTML comme `<p></p>` ou `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="93a57-247">Enclose the text in an HTML element like `<p></p>` or `<em></em>`:</span></span>   

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample12.cshtml)]

    <span data-ttu-id="93a57-248">L’élément HTML peut inclure de texte, des éléments HTML supplémentaires et des expressions de code serveur.</span><span class="sxs-lookup"><span data-stu-id="93a57-248">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="93a57-249">Lorsque ASP.NET reconnaît la balise HTML d’ouverture (par exemple, `<p>`), il affiche tous les éléments, y compris l’élément et son contenu sous la forme est dans le navigateur, résolution des expressions de code serveur lorsqu’il passe.</span><span class="sxs-lookup"><span data-stu-id="93a57-249">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything including the element and its content as is to the browser, resolving server-code expressions as it goes.</span></span>
- <span data-ttu-id="93a57-250">Utilisez le `@:` opérateur ou `<text>` élément.</span><span class="sxs-lookup"><span data-stu-id="93a57-250">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="93a57-251">Le `@:` génère une ligne unique de contenu de texte brut ou des balises HTML non appariées ; le `<text>` élément englobe plusieurs lignes de sortie.</span><span class="sxs-lookup"><span data-stu-id="93a57-251">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="93a57-252">Ces options sont utiles lorsque vous ne souhaitez pas restituer un élément HTML dans le cadre de la sortie.</span><span class="sxs-lookup"><span data-stu-id="93a57-252">These options are useful when you don't want to render an HTML element as part of the output.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample13.cshtml)]

    <span data-ttu-id="93a57-253">Si vous souhaitez plusieurs lignes de texte ou les balises HTML sans correspondance de sortie, vous pouvez le faire précéder chaque ligne avec `@:`, ou vous pouvez placer la ligne dans un `<text>` élément.</span><span class="sxs-lookup"><span data-stu-id="93a57-253">If you want to output multiple lines of text or unmatched HTML tags, you can precede each line with `@:`, or you can enclose the line in a `<text>` element.</span></span> <span data-ttu-id="93a57-254">Comme le `@:` (opérateur),`<text>` balises sont utilisés par ASP.NET pour identifier le contenu de texte et ne sont jamais restitués dans la sortie de page.</span><span class="sxs-lookup"><span data-stu-id="93a57-254">Like the `@:` operator,`<text>` tags are used by ASP.NET to identify text content and are never rendered in the page output.</span></span>

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample14.cshtml)]

    <span data-ttu-id="93a57-255">Le premier exemple répète l’exemple précédent, mais utilise une seule paire de `<text>` balises à placer le texte à afficher.</span><span class="sxs-lookup"><span data-stu-id="93a57-255">The first example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span> <span data-ttu-id="93a57-256">Dans le deuxième exemple, le `<text>` et `</text>` balises placez trois lignes, qui ont des relation contenant-contenu de texte et des balises sans correspondance (`<br />`), ainsi que le code serveur et de mise en correspondance les balises HTML.</span><span class="sxs-lookup"><span data-stu-id="93a57-256">In the second example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="93a57-257">Là encore, vous pourriez également le faire précéder chaque ligne individuellement avec la `@:` opérateur ; les deux fonctionnent de façon.</span><span class="sxs-lookup"><span data-stu-id="93a57-257">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    > [!NOTE]
    > <span data-ttu-id="93a57-258">Sortie de texte comme indiqué dans cette section &#8212; à l’aide d’un élément HTML, la `@:` (opérateur), ou le `<text>` , élément &#8212; ASP.NET ne pas coder en HTML la sortie.</span><span class="sxs-lookup"><span data-stu-id="93a57-258">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="93a57-259">(Comme indiqué précédemment, ASP.NET n’encode pas la sortie des expressions de code serveur et les blocs de code de serveur qui sont précédés par `@`, sauf dans les cas spéciaux dans cette section.)</span><span class="sxs-lookup"><span data-stu-id="93a57-259">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="93a57-260">Whitespace</span><span class="sxs-lookup"><span data-stu-id="93a57-260">Whitespace</span></span>

<span data-ttu-id="93a57-261">L’instruction n’affectent pas les espaces superflus dans une instruction (et en dehors d’un littéral de chaîne) :</span><span class="sxs-lookup"><span data-stu-id="93a57-261">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample15.cshtml)]

<span data-ttu-id="93a57-262">Un saut de ligne dans une instruction n’a aucun effet sur l’instruction, et vous pouvez encapsuler des instructions pour une meilleure lisibilité.</span><span class="sxs-lookup"><span data-stu-id="93a57-262">A line break in a statement has no effect on the statement, and you can wrap statements for readability.</span></span> <span data-ttu-id="93a57-263">Les instructions suivantes sont les mêmes :</span><span class="sxs-lookup"><span data-stu-id="93a57-263">The following statements are the same:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample16.cshtml)]

<span data-ttu-id="93a57-264">Toutefois, vous ne pouvez pas encapsuler une ligne au milieu d’un littéral de chaîne.</span><span class="sxs-lookup"><span data-stu-id="93a57-264">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="93a57-265">L’exemple suivant ne fonctionne pas :</span><span class="sxs-lookup"><span data-stu-id="93a57-265">The following example doesn't work:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample17.cshtml)]

<span data-ttu-id="93a57-266">Pour associer une longue chaîne répartie sur plusieurs lignes, comme le code ci-dessus, il existe deux options.</span><span class="sxs-lookup"><span data-stu-id="93a57-266">To combine a long string that wraps to multiple lines like the above code, there are two options.</span></span> <span data-ttu-id="93a57-267">Vous pouvez utiliser l’opérateur de concaténation (`+`), comme vous le verrez plus loin dans cet article.</span><span class="sxs-lookup"><span data-stu-id="93a57-267">You can use the concatenation operator (`+`), which you'll see later in this article.</span></span> <span data-ttu-id="93a57-268">Vous pouvez également utiliser le `@` caractères pour créer une chaîne textuelle littéral, comme vous l’avez vu plus haut dans cet article.</span><span class="sxs-lookup"><span data-stu-id="93a57-268">You can also use the `@` character to create a verbatim string literal, as you saw earlier in this article.</span></span> <span data-ttu-id="93a57-269">Vous pouvez rompre les littéraux de chaîne textuelle sur plusieurs lignes :</span><span class="sxs-lookup"><span data-stu-id="93a57-269">You can break verbatim string literals across lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample18.cshtml)]

### <a name="code-and-markup-comments"></a><span data-ttu-id="93a57-270">Code (et le balisage) commentaires</span><span class="sxs-lookup"><span data-stu-id="93a57-270">Code (and Markup) Comments</span></span>

<span data-ttu-id="93a57-271">Commentaires vous permettent de rédiger des notes pour vous-même ou d’autres utilisateurs.</span><span class="sxs-lookup"><span data-stu-id="93a57-271">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="93a57-272">Ils vous permettent également de désactiver (*commentez*) une section de code ou de balisage que vous ne souhaitez pas exécuter mais souhaitez conserver dans votre page pour l’instant.</span><span class="sxs-lookup"><span data-stu-id="93a57-272">They also allow you to disable (*comment out*) a section of code or markup that you don't want to run but want to keep in your page for the time being.</span></span>

<span data-ttu-id="93a57-273">Il est différent de la syntaxe pour le code Razor pour le balisage HTML de commentaires.</span><span class="sxs-lookup"><span data-stu-id="93a57-273">There's different commenting syntax for Razor code and for HTML markup.</span></span> <span data-ttu-id="93a57-274">Comme avec tout le code Razor, Razor commentaires sont traités (et puis supprimées) sur le serveur avant que la page est envoyée au navigateur.</span><span class="sxs-lookup"><span data-stu-id="93a57-274">As with all Razor code, Razor comments are processed (and then removed) on the server before the page is sent to the browser.</span></span> <span data-ttu-id="93a57-275">Par conséquent, la syntaxe de commentaire Razor vous permet de placer des commentaires dans le code (ou même dans le balisage) que vous pouvez voir lorsque vous modifiez le fichier, mais que les utilisateurs ne voient, même dans la page source.</span><span class="sxs-lookup"><span data-stu-id="93a57-275">Therefore, the Razor commenting syntax lets you put comments into the code (or even into the markup) that you can see when you edit the file, but that users don't see, even in the page source.</span></span>

<span data-ttu-id="93a57-276">Pour les commentaires de ASP.NET Razor, vous commencez le commentaire avec `@*` et se terminer avec `*@`.</span><span class="sxs-lookup"><span data-stu-id="93a57-276">For ASP.NET Razor comments, you start the comment with `@*` and end it with `*@`.</span></span> <span data-ttu-id="93a57-277">Ce commentaire peut être sur une ou plusieurs lignes :</span><span class="sxs-lookup"><span data-stu-id="93a57-277">The comment can be on one line or multiple lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample19.cshtml)]

<span data-ttu-id="93a57-278">Voici un commentaire dans un bloc de code :</span><span class="sxs-lookup"><span data-stu-id="93a57-278">Here is a comment within a code block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample20.cshtml)]

<span data-ttu-id="93a57-279">Ici est le même bloc de code, avec la ligne de code commenté afin qu’elle ne sera pas exécutée :</span><span class="sxs-lookup"><span data-stu-id="93a57-279">Here is the same block of code, with the line of code commented out so that it won't run:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample21.cshtml)]

<span data-ttu-id="93a57-280">À l’intérieur d’un bloc de code, comme alternative à l’aide de syntaxe de commentaire Razor, vous pouvez utiliser la syntaxe de commentaire du langage de programmation que vous utilisez des, tels que c# :</span><span class="sxs-lookup"><span data-stu-id="93a57-280">Inside a code block, as an alternative to using Razor comment syntax, you can use the commenting syntax of the programming language you're using, such as C#:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample22.cshtml)]

<span data-ttu-id="93a57-281">En c#, les commentaires sur une ligne sont précédées par le `//` caractères et les commentaires de plusieurs lignes commencent par `/*` et se terminer par `*/`.</span><span class="sxs-lookup"><span data-stu-id="93a57-281">In C#, single-line comments are preceded by the `//` characters, and multi-line comments begin with `/*` and end with `*/`.</span></span> <span data-ttu-id="93a57-282">(Comme avec les commentaires de Razor, les commentaires c# ne sont pas rendus dans le navigateur.)</span><span class="sxs-lookup"><span data-stu-id="93a57-282">(As with Razor comments, C# comments are not rendered to the browser.)</span></span>

<span data-ttu-id="93a57-283">Pour le balisage, comme vous le savez, vous pouvez créer un commentaire HTML :</span><span class="sxs-lookup"><span data-stu-id="93a57-283">For markup, as you probably know, you can create an HTML comment:</span></span>

[!code-xml[Main](introducing-razor-syntax-c/samples/sample23.xml)]

<span data-ttu-id="93a57-284">Commentaires HTML commencent par `<!--` caractères et se terminent par `-->`.</span><span class="sxs-lookup"><span data-stu-id="93a57-284">HTML comments start with `<!--` characters and end with `-->`.</span></span> <span data-ttu-id="93a57-285">Vous pouvez utiliser des commentaires HTML pour entourer non seulement de texte, mais également toutes les balises HTML que vous souhaitez peut-être conserver dans la page mais que vous ne souhaitez pas effectuer le rendu.</span><span class="sxs-lookup"><span data-stu-id="93a57-285">You can use HTML comments to surround not only text, but also any HTML markup that you may want to keep in the page but don't want to render.</span></span> <span data-ttu-id="93a57-286">Ce commentaire HTML masquera l’ensemble du contenu des balises et le texte qu’ils contiennent :</span><span class="sxs-lookup"><span data-stu-id="93a57-286">This HTML comment will hide the entire content of the tags and the text they contain:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample24.html)]

<span data-ttu-id="93a57-287">Contrairement aux commentaires Razor, commentaires HTML *sont* restitué dans la page et l’utilisateur peut les voir en affichant la source de la page.</span><span class="sxs-lookup"><span data-stu-id="93a57-287">Unlike Razor comments, HTML comments *are* rendered to the page and the user can see them by viewing the page source.</span></span>

<span data-ttu-id="93a57-288">Razor comporte des limitations sur les blocs imbriqués de c#.</span><span class="sxs-lookup"><span data-stu-id="93a57-288">Razor has limitations on nested blocks of C#.</span></span> <span data-ttu-id="93a57-289">Pour plus d’informations, consultez [Variables c# nommé et imbriqués blocs générer rompu le Code](http://aspnetwebstack.codeplex.com/workitem/1914)</span><span class="sxs-lookup"><span data-stu-id="93a57-289">For more information see [Named C# Variables and Nested Blocks Generate Broken Code](http://aspnetwebstack.codeplex.com/workitem/1914)</span></span>

## <a name="variables"></a><span data-ttu-id="93a57-290">Variables</span><span class="sxs-lookup"><span data-stu-id="93a57-290">Variables</span></span>

<span data-ttu-id="93a57-291">Une variable est un objet nommé qui vous permet de stocker des données.</span><span class="sxs-lookup"><span data-stu-id="93a57-291">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="93a57-292">Vous pouvez nommer les variables, mais le nom doit commencer par un caractère alphabétique, et il ne peut pas contenir d’espaces blancs ou des caractères réservés.</span><span class="sxs-lookup"><span data-stu-id="93a57-292">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="93a57-293">Les variables et les Types de données</span><span class="sxs-lookup"><span data-stu-id="93a57-293">Variables and Data Types</span></span>

<span data-ttu-id="93a57-294">Une variable peut avoir un type de données spécifique, ce qui indique le type de données est stocké dans la variable.</span><span class="sxs-lookup"><span data-stu-id="93a57-294">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="93a57-295">Vous pouvez avoir des variables de chaîne qui stockent les valeurs de chaîne (comme &quot;Hello world&quot;), les variables de type entier qui stockent des valeurs de nombre entier (comme 3 ou 79) et les variables de date qui stockent les valeurs de date dans divers formats (par exemple, 12/4/2012 ou mars 2009 ).</span><span class="sxs-lookup"><span data-stu-id="93a57-295">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="93a57-296">Et il existe de nombreux autres types de données que vous pouvez utiliser.</span><span class="sxs-lookup"><span data-stu-id="93a57-296">And there are many other data types you can use.</span></span>

<span data-ttu-id="93a57-297">Toutefois, généralement inutile de spécifier un type d’une variable.</span><span class="sxs-lookup"><span data-stu-id="93a57-297">However, you generally don't have to specify a type for a variable.</span></span> <span data-ttu-id="93a57-298">La plupart du temps, ASP.NET peut déterminer le type en fonction de l’utilisation de données dans la variable.</span><span class="sxs-lookup"><span data-stu-id="93a57-298">Most of the time, ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="93a57-299">(Il peut arriver que vous devez spécifier un type ; vous allez voir des exemples où cela est vrai.)</span><span class="sxs-lookup"><span data-stu-id="93a57-299">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="93a57-300">Vous déclarez une variable à l’aide du `var` (mot clé) (si vous ne souhaitez pas spécifier un type) ou en utilisant le nom du type :</span><span class="sxs-lookup"><span data-stu-id="93a57-300">You declare a variable using the `var` keyword (if you don't want to specify a type) or by using the name of the type:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample25.cshtml)]

<span data-ttu-id="93a57-301">L’exemple suivant montre certaines utilisations courantes des variables dans une page web :</span><span class="sxs-lookup"><span data-stu-id="93a57-301">The following example shows some typical uses of variables in a web page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample26.cshtml)]

<span data-ttu-id="93a57-302">Si vous combinez les exemples précédents dans une page, vous consultez cette affiché dans un navigateur :</span><span class="sxs-lookup"><span data-stu-id="93a57-302">If you combine the previous examples in a page, you see this displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-c/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="93a57-304">Conversion et le test des Types de données</span><span class="sxs-lookup"><span data-stu-id="93a57-304">Converting and Testing Data Types</span></span>

<span data-ttu-id="93a57-305">Bien que ASP.NET peut généralement déterminer un type de données automatiquement, parfois, il ne peut pas.</span><span class="sxs-lookup"><span data-stu-id="93a57-305">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="93a57-306">Par conséquent, vous devrez peut-être aider ASP.NET en effectuant une conversion explicite.</span><span class="sxs-lookup"><span data-stu-id="93a57-306">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="93a57-307">Même si vous n’avez pas à convertir des types, il est parfois utile de test pour voir quels types de données vous travaillez avec.</span><span class="sxs-lookup"><span data-stu-id="93a57-307">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="93a57-308">Le cas le plus courant est que vous devez convertir une chaîne en un autre type, comme un entier ou une date.</span><span class="sxs-lookup"><span data-stu-id="93a57-308">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="93a57-309">L’exemple suivant illustre un cas classique où vous devez convertir une chaîne en un nombre.</span><span class="sxs-lookup"><span data-stu-id="93a57-309">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample27.cshtml)]

<span data-ttu-id="93a57-310">En règle générale, l’entrée d’utilisateur est fourni pour vous en tant que chaînes.</span><span class="sxs-lookup"><span data-stu-id="93a57-310">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="93a57-311">Même si vous avez invité à fournir aux utilisateurs d’entrer un nombre, et même s’ils ont entré un chiffre, lors de l’entrée d’utilisateur est soumise et lisez-le dans le code, les données sont au format de chaîne.</span><span class="sxs-lookup"><span data-stu-id="93a57-311">Even if you've prompted users to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="93a57-312">Par conséquent, vous devez convertir la chaîne en un nombre.</span><span class="sxs-lookup"><span data-stu-id="93a57-312">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="93a57-313">Dans l’exemple, si vous essayez d’effectuer une opération arithmétique sur les valeurs sans avoir à les convertir, l’erreur suivante se produit, car ASP.NET ne peut pas ajouter les deux chaînes :</span><span class="sxs-lookup"><span data-stu-id="93a57-313">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

<span data-ttu-id="93a57-314">*Impossible de convertir implicitement le type 'string' en 'int'.*</span><span class="sxs-lookup"><span data-stu-id="93a57-314">*Cannot implicitly convert type 'string' to 'int'.*</span></span>

<span data-ttu-id="93a57-315">Pour convertir les valeurs à des entiers, vous appelez le `AsInt` (méthode).</span><span class="sxs-lookup"><span data-stu-id="93a57-315">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="93a57-316">Si la conversion est réussie, vous pouvez ensuite ajouter les numéros.</span><span class="sxs-lookup"><span data-stu-id="93a57-316">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="93a57-317">Le tableau suivant répertorie certaines méthodes courantes de conversion et de test pour les variables.</span><span class="sxs-lookup"><span data-stu-id="93a57-317">The following table lists some common conversion and test methods for variables.</span></span>

| <span data-ttu-id="93a57-318">**Méthode**</span><span class="sxs-lookup"><span data-stu-id="93a57-318">**Method**</span></span> | <span data-ttu-id="93a57-319">**Description**</span><span class="sxs-lookup"><span data-stu-id="93a57-319">**Description**</span></span> | <span data-ttu-id="93a57-320">**Exemple**</span><span class="sxs-lookup"><span data-stu-id="93a57-320">**Example**</span></span> |
| --- | --- | --- |
| `AsInt(), IsInt()` | <span data-ttu-id="93a57-321">Convertit une chaîne qui représente un nombre entier (par exemple, « 593 ») en un entier.</span><span class="sxs-lookup"><span data-stu-id="93a57-321">Converts a string that represents a whole number (like "593") to an integer.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample28.cs)] |
| `AsBool(), IsBool()` | <span data-ttu-id="93a57-322">Convertit une chaîne like &quot;true&quot; ou &quot;false&quot; avec un type Boolean.</span><span class="sxs-lookup"><span data-stu-id="93a57-322">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample29.cs)] |
| `AsFloat(), IsFloat()` | <span data-ttu-id="93a57-323">Convertit une chaîne qui a une valeur décimale comme &quot;1.3&quot; ou &quot;7.439&quot; un nombre à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="93a57-323">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample30.cs)] |
| `AsDecimal(), IsDecimal()` | <span data-ttu-id="93a57-324">Convertit une chaîne qui a une valeur décimale comme &quot;1.3&quot; ou &quot;7.439&quot; en un nombre décimal.</span><span class="sxs-lookup"><span data-stu-id="93a57-324">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="93a57-325">(Dans ASP.NET, un nombre décimal est plus précis que nombre à virgule flottante.)</span><span class="sxs-lookup"><span data-stu-id="93a57-325">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample31.cs)] |
| `AsDateTime(), IsDateTime()` | <span data-ttu-id="93a57-326">Convertit une chaîne qui représente une valeur de date et d’heure pour ASP.NET `DateTime` type.</span><span class="sxs-lookup"><span data-stu-id="93a57-326">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample32.cs)] |
| `ToString()` | <span data-ttu-id="93a57-327">Convertit un autre type de données en une chaîne.</span><span class="sxs-lookup"><span data-stu-id="93a57-327">Converts any other data type to a string.</span></span> | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample33.js)] |

## <a name="operators"></a><span data-ttu-id="93a57-328">Opérateurs</span><span class="sxs-lookup"><span data-stu-id="93a57-328">Operators</span></span>

<span data-ttu-id="93a57-329">Un opérateur est un mot clé ou un caractère qui indique à ASP.NET de quel type de commande à exécuter dans une expression.</span><span class="sxs-lookup"><span data-stu-id="93a57-329">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="93a57-330">Langage c# (et la syntaxe Razor qui est basée sur ce dernier) prend en charge de nombreux opérateurs, mais vous devez seulement reconnaître quelques exemples pour commencer.</span><span class="sxs-lookup"><span data-stu-id="93a57-330">The C# language (and the Razor syntax that's based on it) supports many operators, but you only need to recognize a few to get started.</span></span> <span data-ttu-id="93a57-331">Le tableau suivant récapitule les opérateurs courants.</span><span class="sxs-lookup"><span data-stu-id="93a57-331">The following table summarizes the most common operators.</span></span>

| <span data-ttu-id="93a57-332">**Operator**</span><span class="sxs-lookup"><span data-stu-id="93a57-332">**Operator**</span></span> | <span data-ttu-id="93a57-333">**Description**</span><span class="sxs-lookup"><span data-stu-id="93a57-333">**Description**</span></span> | <span data-ttu-id="93a57-334">**Exemples**</span><span class="sxs-lookup"><span data-stu-id="93a57-334">**Examples**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="93a57-335">`+` `-` `*` `/`</span><span class="sxs-lookup"><span data-stu-id="93a57-335">`+` `-` `*` `/`</span></span> | <span data-ttu-id="93a57-336">Opérateurs mathématiques utilisés dans les expressions numériques.</span><span class="sxs-lookup"><span data-stu-id="93a57-336">Math operators used in numerical expressions.</span></span> | [!code-css[Main](introducing-razor-syntax-c/samples/sample34.css)] |
| `=` | <span data-ttu-id="93a57-337">Assignation.</span><span class="sxs-lookup"><span data-stu-id="93a57-337">Assignment.</span></span> <span data-ttu-id="93a57-338">Assigne la valeur située à droite d’une instruction à l’objet sur le côté gauche.</span><span class="sxs-lookup"><span data-stu-id="93a57-338">Assigns the value on the right side of a statement to the object on the left side.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample35.cs)] |
| `==` | <span data-ttu-id="93a57-339">Égalité</span><span class="sxs-lookup"><span data-stu-id="93a57-339">Equality.</span></span> <span data-ttu-id="93a57-340">Retourne `true` si les valeurs sont égales.</span><span class="sxs-lookup"><span data-stu-id="93a57-340">Returns `true` if the values are equal.</span></span> <span data-ttu-id="93a57-341">(Notez la différence entre la `=` opérateur et la `==` opérateur.)</span><span class="sxs-lookup"><span data-stu-id="93a57-341">(Notice the distinction between the `=` operator and the `==` operator.)</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample36.cs)] |
| `!=` | <span data-ttu-id="93a57-342">Inégalité</span><span class="sxs-lookup"><span data-stu-id="93a57-342">Inequality.</span></span> <span data-ttu-id="93a57-343">Retourne `true` si les valeurs ne sont pas égales.</span><span class="sxs-lookup"><span data-stu-id="93a57-343">Returns `true` if the values are not equal.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample37.cs)] |
| `< > <= >=` | <span data-ttu-id="93a57-344">Moins-supérieur-à, inférieur ou égal et supérieur ou égal.</span><span class="sxs-lookup"><span data-stu-id="93a57-344">Less-than, greater-than, less-than-or-equal, and greater-than-or-equal.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample38.cs)] |
| `+` | <span data-ttu-id="93a57-345">Concaténation, qui est utilisée pour joindre des chaînes.</span><span class="sxs-lookup"><span data-stu-id="93a57-345">Concatenation, which is used to join strings.</span></span> <span data-ttu-id="93a57-346">ASP.NET connaît la différence entre cet opérateur et l’opérateur d’addition en fonction du type de données de l’expression.</span><span class="sxs-lookup"><span data-stu-id="93a57-346">ASP.NET knows the difference between this operator and the addition operator based on the data type of the expression.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample39.cs)] |
| <span data-ttu-id="93a57-347">`+=` `-=`</span><span class="sxs-lookup"><span data-stu-id="93a57-347">`+=` `-=`</span></span> | <span data-ttu-id="93a57-348">Les opérateurs incrémentation et de décrémentation, lequel ajouter ou soustraire 1 (respectivement) à partir d’une variable.</span><span class="sxs-lookup"><span data-stu-id="93a57-348">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample40.cs)] |
| `.` | <span data-ttu-id="93a57-349">Point.</span><span class="sxs-lookup"><span data-stu-id="93a57-349">Dot.</span></span> <span data-ttu-id="93a57-350">Utilisé pour distinguer les objets et leurs propriétés et les méthodes.</span><span class="sxs-lookup"><span data-stu-id="93a57-350">Used to distinguish objects and their properties and methods.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample41.cs)] |
| `()` | <span data-ttu-id="93a57-351">Entre parenthèses.</span><span class="sxs-lookup"><span data-stu-id="93a57-351">Parentheses.</span></span> <span data-ttu-id="93a57-352">Utilisé pour les expressions de groupe et pour passer des paramètres aux méthodes.</span><span class="sxs-lookup"><span data-stu-id="93a57-352">Used to group expressions and to pass parameters to methods.</span></span> | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample42.js)] |
| `[]` | <span data-ttu-id="93a57-353">Des crochets.</span><span class="sxs-lookup"><span data-stu-id="93a57-353">Brackets.</span></span> <span data-ttu-id="93a57-354">Utilisé pour accéder aux valeurs dans les tableaux ou collections.</span><span class="sxs-lookup"><span data-stu-id="93a57-354">Used for accessing values in arrays or collections.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample43.cs)] |
| `!` | <span data-ttu-id="93a57-355">Non.</span><span class="sxs-lookup"><span data-stu-id="93a57-355">Not.</span></span> <span data-ttu-id="93a57-356">Inverse un `true` valeur `false` et vice versa.</span><span class="sxs-lookup"><span data-stu-id="93a57-356">Reverses a `true` value to `false` and vice versa.</span></span> <span data-ttu-id="93a57-357">Généralement utilisé comme un moyen rapide pour tester le `false` (autrement dit, pour pas `true`).</span><span class="sxs-lookup"><span data-stu-id="93a57-357">Typically used as a shorthand way to test for `false` (that is, for not `true`).</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample44.cs)] |
| <span data-ttu-id="93a57-358">`&&` <code>&#124;&#124;</code></span><span class="sxs-lookup"><span data-stu-id="93a57-358">`&&` <code>&#124;&#124;</code></span></span> | <span data-ttu-id="93a57-359">ET logique et ou des conditions qui servent à lier.</span><span class="sxs-lookup"><span data-stu-id="93a57-359">Logical AND and OR, which are used to link conditions together.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample45.cs)] |

<a id="ID_WorkingWithFileAndFolderPaths"></a>
## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="93a57-360">Utilisation des fichiers et des chemins d’accès de dossier dans le Code</span><span class="sxs-lookup"><span data-stu-id="93a57-360">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="93a57-361">Souvent, vous allez travailler avec les chemins d’accès de fichier et de dossier dans votre code.</span><span class="sxs-lookup"><span data-stu-id="93a57-361">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="93a57-362">Voici un exemple de structure de dossier physique pour un site Web tel qu’il peut apparaître sur votre ordinateur de développement :</span><span class="sxs-lookup"><span data-stu-id="93a57-362">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="93a57-363">Voici certaines informations essentielles sur les URL et les chemins d’accès :</span><span class="sxs-lookup"><span data-stu-id="93a57-363">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="93a57-364">Une URL commence par un nom de domaine (`http://www.example.com`) ou un nom de serveur (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="93a57-364">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="93a57-365">Une URL correspond à un chemin d’accès physique sur un ordinateur hôte.</span><span class="sxs-lookup"><span data-stu-id="93a57-365">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="93a57-366">Par exemple, `http://myserver` peut correspondre au dossier *C:\websites\mywebsite* sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="93a57-366">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="93a57-367">Un chemin d’accès virtuel est l’abréviation pour représenter les chemins d’accès dans le code sans avoir à spécifier le chemin d’accès complet.</span><span class="sxs-lookup"><span data-stu-id="93a57-367">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="93a57-368">Il inclut la partie d’une URL qui suit le nom de domaine ou du serveur.</span><span class="sxs-lookup"><span data-stu-id="93a57-368">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="93a57-369">Lorsque vous utilisez des chemins d’accès virtuels, vous pouvez déplacer votre code vers un autre domaine ou un serveur sans avoir à mettre à jour les chemins d’accès.</span><span class="sxs-lookup"><span data-stu-id="93a57-369">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="93a57-370">Voici un exemple pour vous aider à comprendre les différences :</span><span class="sxs-lookup"><span data-stu-id="93a57-370">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="93a57-371">URL complète</span><span class="sxs-lookup"><span data-stu-id="93a57-371">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="93a57-372">Nom du serveur</span><span class="sxs-lookup"><span data-stu-id="93a57-372">Server name</span></span> | <span data-ttu-id="93a57-373">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="93a57-373">*mycompanyserver*</span></span> |
| <span data-ttu-id="93a57-374">Chemin d'accès virtuel</span><span class="sxs-lookup"><span data-stu-id="93a57-374">Virtual path</span></span> | <span data-ttu-id="93a57-375">*/humanresources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="93a57-375">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="93a57-376">Chemin d’accès physique</span><span class="sxs-lookup"><span data-stu-id="93a57-376">Physical path</span></span> | <span data-ttu-id="93a57-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="93a57-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="93a57-378">La racine virtuelle est /, tout comme la racine de votre lecteur est \.</span><span class="sxs-lookup"><span data-stu-id="93a57-378">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="93a57-379">(Chemins d’accès du dossier virtuel utilisent toujours des barres obliques.) Le chemin d’accès virtuel d’un dossier ne doit pas nécessairement avoir le même nom que le dossier physique ; Il peut être un alias.</span><span class="sxs-lookup"><span data-stu-id="93a57-379">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="93a57-380">(Sur les serveurs de production, le chemin d’accès virtuel rarement correspond à un chemin d’accès physique exact.)</span><span class="sxs-lookup"><span data-stu-id="93a57-380">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="93a57-381">Lorsque vous travaillez avec des fichiers et dossiers dans le code, vous devez parfois référencer le chemin d’accès physique et parfois selon les objets que vous travaillez avec un chemin d’accès virtuel.</span><span class="sxs-lookup"><span data-stu-id="93a57-381">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="93a57-382">ASP.NET vous donne les outils pour l’utilisation des chemins d’accès de fichier et de dossier dans le code : le `Server.MapPath` (méthode) et le `~` opérateur et `Href` (méthode).</span><span class="sxs-lookup"><span data-stu-id="93a57-382">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="93a57-383">Conversion des chemins d’accès virtuels physiques : la méthode Server.MapPath</span><span class="sxs-lookup"><span data-stu-id="93a57-383">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="93a57-384">Le `Server.MapPath` méthode convertit un chemin d’accès virtuel (comme */default.cshtml*) à un chemin d’accès physique absolu (comme *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="93a57-384">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="93a57-385">Vous utilisez cette méthode à tout moment, que vous avez besoin d’un chemin d’accès physique complet.</span><span class="sxs-lookup"><span data-stu-id="93a57-385">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="93a57-386">Un exemple classique est lors de la lecture ou l’écriture d’un fichier texte ou un fichier image sur le serveur web.</span><span class="sxs-lookup"><span data-stu-id="93a57-386">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="93a57-387">En général, vous ne connaissez pas de chemin d’accès physique absolu de votre site sur le serveur d’hébergement d’un site, et cette méthode peut convertir le chemin d’accès que vous ne connaissez : le chemin d’accès virtuel, le chemin d’accès correspondants sur le serveur pour vous.</span><span class="sxs-lookup"><span data-stu-id="93a57-387">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="93a57-388">Vous passez le chemin d’accès virtuel à un fichier ou dossier à la méthode et retourne le chemin d’accès physique :</span><span class="sxs-lookup"><span data-stu-id="93a57-388">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample46.cshtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="93a57-389">Faisant référence à la racine virtuelle : le ~ opérateur et Href (méthode)</span><span class="sxs-lookup"><span data-stu-id="93a57-389">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="93a57-390">Dans un *.cshtml* ou *.vbhtml* fichier, vous pouvez référencer le chemin d’accès virtuel racine à l’aide du `~` opérateur.</span><span class="sxs-lookup"><span data-stu-id="93a57-390">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="93a57-391">Cela est très pratique, car vous pouvez déplacer les pages dans un site et tous les liens qu’ils contiennent dans d’autres pages ne pourra plus être interrompues.</span><span class="sxs-lookup"><span data-stu-id="93a57-391">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="93a57-392">Il est également utile si vous décidez de déplacer votre site Web vers un autre emplacement.</span><span class="sxs-lookup"><span data-stu-id="93a57-392">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="93a57-393">Voici quelques exemples :</span><span class="sxs-lookup"><span data-stu-id="93a57-393">Here are some examples:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample47.cshtml)]

<span data-ttu-id="93a57-394">Si le site Web est `http://myserver/myapp`, voici comment ASP.NET traite ces chemins d’accès lors de l’exécution de la page :</span><span class="sxs-lookup"><span data-stu-id="93a57-394">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="93a57-395">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="93a57-395">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="93a57-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="93a57-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="93a57-397">(Vous ne voyez pas réellement ces chemins d’accès en tant que les valeurs de la variable, mais ASP.NET traite les chemins d’accès comme si c’est qu’ils étaient).</span><span class="sxs-lookup"><span data-stu-id="93a57-397">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="93a57-398">Vous pouvez utiliser la `~` opérateur à la fois dans le code serveur (comme ci-dessus) et dans le balisage, comme suit :</span><span class="sxs-lookup"><span data-stu-id="93a57-398">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample48.html)]

<span data-ttu-id="93a57-399">Dans le balisage, vous utilisez la `~` opérateur pour créer des chemins d’accès aux ressources telles que des fichiers image, d’autres pages web et de fichiers CSS.</span><span class="sxs-lookup"><span data-stu-id="93a57-399">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="93a57-400">Lorsque la page s’exécute, ASP.NET recherche dans la page (de code et de balisage) et résout tous les `~` références au chemin approprié.</span><span class="sxs-lookup"><span data-stu-id="93a57-400">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="93a57-401">Boucles et la logique conditionnelle</span><span class="sxs-lookup"><span data-stu-id="93a57-401">Conditional Logic and Loops</span></span>

<span data-ttu-id="93a57-402">Code de serveur ASP.NET vous permet d’effectuer des tâches en fonction des conditions et d’écrire du code qui répète des instructions un certain nombre de fois (autrement dit, code qui exécute une boucle).</span><span class="sxs-lookup"><span data-stu-id="93a57-402">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times (that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="93a57-403">Conditions de test</span><span class="sxs-lookup"><span data-stu-id="93a57-403">Testing Conditions</span></span>

<span data-ttu-id="93a57-404">Pour tester une condition simple que vous utilisez la `if` instruction, qui retourne true ou false selon un test, vous spécifiez :</span><span class="sxs-lookup"><span data-stu-id="93a57-404">To test a simple condition you use the `if` statement, which returns true or false based on a test you specify:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample49.cshtml)]

<span data-ttu-id="93a57-405">Le `if` mot clé commence un bloc.</span><span class="sxs-lookup"><span data-stu-id="93a57-405">The `if` keyword starts a block.</span></span> <span data-ttu-id="93a57-406">Le test (condition) figure entre parenthèses et retourne true ou false.</span><span class="sxs-lookup"><span data-stu-id="93a57-406">The actual test (condition) is in parentheses and returns true or false.</span></span> <span data-ttu-id="93a57-407">Les instructions qui s’exécutent si le test a la valeur true sont placées entre accolades.</span><span class="sxs-lookup"><span data-stu-id="93a57-407">The statements that run if the test is true are enclosed in braces.</span></span> <span data-ttu-id="93a57-408">Un `if` instruction peut inclure un `else` block qui spécifie les instructions à exécuter si la condition a la valeur false :</span><span class="sxs-lookup"><span data-stu-id="93a57-408">An `if` statement can include an `else` block that specifies statements to run if the condition is false:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample50.cshtml)]

<span data-ttu-id="93a57-409">Vous pouvez ajouter plusieurs conditions à l’aide un `else if` bloc :</span><span class="sxs-lookup"><span data-stu-id="93a57-409">You can add multiple conditions using an `else if` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample51.cshtml)]

<span data-ttu-id="93a57-410">Dans cet exemple, si la première condition dans le cas bloc n’est pas la valeur est true, le `else if` condition est vérifiée.</span><span class="sxs-lookup"><span data-stu-id="93a57-410">In this example, if the first condition in the if block is not true, the `else if` condition is checked.</span></span> <span data-ttu-id="93a57-411">Si cette condition est remplie, les instructions figurant dans le `else if` bloc sont exécutées.</span><span class="sxs-lookup"><span data-stu-id="93a57-411">If that condition is met, the statements in the `else if` block are executed.</span></span> <span data-ttu-id="93a57-412">Si aucune des conditions sont remplies, les instructions figurant dans le `else` bloc sont exécutées.</span><span class="sxs-lookup"><span data-stu-id="93a57-412">If none of the conditions are met, the statements in the `else` block are executed.</span></span> <span data-ttu-id="93a57-413">Vous pouvez ajouter un nombre quelconque de Sinon si bloque et fermez avec un `else` bloquer le &quot;tout le reste&quot; condition.</span><span class="sxs-lookup"><span data-stu-id="93a57-413">You can add any number of else if blocks, and then close with an `else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="93a57-414">Pour tester un grand nombre de conditions, utilisez un `switch` bloc :</span><span class="sxs-lookup"><span data-stu-id="93a57-414">To test a large number of conditions, use a `switch` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample52.cshtml)]

<span data-ttu-id="93a57-415">La valeur à tester est entre parenthèses (dans l’exemple, le `weekday` variable).</span><span class="sxs-lookup"><span data-stu-id="93a57-415">The value to test is in parentheses (in the example, the `weekday` variable).</span></span> <span data-ttu-id="93a57-416">Chaque test individuel utilise un `case` instruction se termine par un signe deux-points ( :).</span><span class="sxs-lookup"><span data-stu-id="93a57-416">Each individual test uses a `case` statement that ends with a colon (:).</span></span> <span data-ttu-id="93a57-417">Si la valeur d’un `case` instruction correspond à la valeur de test, le code dans ce cas bloc est exécuté.</span><span class="sxs-lookup"><span data-stu-id="93a57-417">If the value of a `case` statement matches the test value, the code in that case block is executed.</span></span> <span data-ttu-id="93a57-418">Vous fermez chaque instruction case avec un `break` instruction.</span><span class="sxs-lookup"><span data-stu-id="93a57-418">You close each case statement with a `break` statement.</span></span> <span data-ttu-id="93a57-419">(Si vous oubliez d’inclure un saut dans chaque `case` bloquer, le code de l’élément suivant `case` instruction s’exécute également.) A `switch` bloc contient souvent un `default` en tant que le dernier cas pour un &quot;tout le reste&quot; option qui s’exécute si aucun des autres cas sont remplies.</span><span class="sxs-lookup"><span data-stu-id="93a57-419">(If you forget to include break in each `case` block, the code from the next `case` statement will run also.) A `switch` block often has a `default` statement as the last case for an &quot;everything else&quot; option that runs if none of the other cases are true.</span></span>

<span data-ttu-id="93a57-420">Le résultat des deux derniers blocs conditionnels affiché dans un navigateur :</span><span class="sxs-lookup"><span data-stu-id="93a57-420">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-c/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="93a57-422">Code de boucle</span><span class="sxs-lookup"><span data-stu-id="93a57-422">Looping Code</span></span>

<span data-ttu-id="93a57-423">Souvent, vous devez exécuter les mêmes instructions à plusieurs reprises.</span><span class="sxs-lookup"><span data-stu-id="93a57-423">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="93a57-424">Pour cela, en effectuant une boucle.</span><span class="sxs-lookup"><span data-stu-id="93a57-424">You do this by looping.</span></span> <span data-ttu-id="93a57-425">Par exemple, vous exécutez souvent les mêmes instructions pour chaque élément dans une collection de données.</span><span class="sxs-lookup"><span data-stu-id="93a57-425">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="93a57-426">Si vous savez exactement combien de fois que vous souhaitez effectuer une boucle, vous pouvez utiliser un `for` boucle.</span><span class="sxs-lookup"><span data-stu-id="93a57-426">If you know exactly how many times you want to loop, you can use a `for` loop.</span></span> <span data-ttu-id="93a57-427">Ce type de boucle est particulièrement utile pour le comptage ou le compte à rebours :</span><span class="sxs-lookup"><span data-stu-id="93a57-427">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample53.html)]

<span data-ttu-id="93a57-428">La boucle commence par le `for` (mot clé), suivie de trois instructions entre parenthèses, chacun termine par un point-virgule.</span><span class="sxs-lookup"><span data-stu-id="93a57-428">The loop begins with the `for` keyword, followed by three statements in parentheses, each terminated with a semicolon.</span></span>

- <span data-ttu-id="93a57-429">À l’intérieur des parenthèses, la première instruction (`var i=10;`) crée un compteur et l’initialise à 10.</span><span class="sxs-lookup"><span data-stu-id="93a57-429">Inside the parentheses, the first statement (`var i=10;`) creates a counter and initializes it to 10.</span></span> <span data-ttu-id="93a57-430">Vous n’êtes pas obligé de nommer le compteur `i` &#8212; vous pouvez utiliser n’importe quelle variable.</span><span class="sxs-lookup"><span data-stu-id="93a57-430">You don't have to name the counter `i` &#8212; you can use any variable.</span></span> <span data-ttu-id="93a57-431">Lorsque le `for` boucle s’exécute, le compteur est incrémenté automatiquement.</span><span class="sxs-lookup"><span data-stu-id="93a57-431">When the `for` loop runs, the counter is automatically incremented.</span></span>
- <span data-ttu-id="93a57-432">La deuxième instruction (`i < 21;`) définit la condition pour déterminer jusqu’où vous souhaitez compter.</span><span class="sxs-lookup"><span data-stu-id="93a57-432">The second statement (`i < 21;`) sets the condition for how far you want to count.</span></span> <span data-ttu-id="93a57-433">Dans ce cas, vous souhaitez qu’il pour accéder à un maximum de 20 (autrement dit, poursuivez la lecture pendant que le compteur est inférieur à 21).</span><span class="sxs-lookup"><span data-stu-id="93a57-433">In this case, you want it to go to a maximum of 20 (that is, keep going while the counter is less than 21).</span></span>
- <span data-ttu-id="93a57-434">La troisième instruction (`i++` ) utilise un opérateur d’incrément, qui spécifie simplement que le compteur doit avoir 1 ajouté à chaque exécution de la boucle.</span><span class="sxs-lookup"><span data-stu-id="93a57-434">The third statement (`i++` ) uses an increment operator, which simply specifies that the counter should have 1 added to it each time the loop runs.</span></span>

<span data-ttu-id="93a57-435">Les accolades est le code qui s’exécute pour chaque itération de la boucle.</span><span class="sxs-lookup"><span data-stu-id="93a57-435">Inside the braces is the code that will run for each iteration of the loop.</span></span> <span data-ttu-id="93a57-436">Le balisage crée un nouveau paragraphe (`<p>` élément) chaque fois, ajoute une ligne à la sortie, la valeur de `i` (le compteur).</span><span class="sxs-lookup"><span data-stu-id="93a57-436">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of `i` (the counter).</span></span> <span data-ttu-id="93a57-437">Lorsque vous exécutez cette page, l’exemple crée 11 lignes affichant la sortie, avec le texte de chaque ligne indiquant le numéro d’article.</span><span class="sxs-lookup"><span data-stu-id="93a57-437">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-c/_static/image11.jpg)

<span data-ttu-id="93a57-439">Si vous travaillez avec une collection ou un tableau, vous utilisez souvent un `foreach` boucle.</span><span class="sxs-lookup"><span data-stu-id="93a57-439">If you're working with a collection or array, you often use a `foreach` loop.</span></span> <span data-ttu-id="93a57-440">Une collection est un groupe d’objets similaires et le `foreach` boucle permet d’effectuer une tâche sur chaque élément dans la collection.</span><span class="sxs-lookup"><span data-stu-id="93a57-440">A collection is a group of similar objects, and the `foreach` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="93a57-441">Ce type de boucle est pratique pour les collections, car contrairement à un `for` boucle, vous n’êtes pas obligé incrément du compteur ou de définir une limite.</span><span class="sxs-lookup"><span data-stu-id="93a57-441">This type of loop is convenient for collections, because unlike a `for` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="93a57-442">Au lieu de cela, le `foreach` code de la boucle passe simplement dans la collection jusqu'à ce qu’il est terminé.</span><span class="sxs-lookup"><span data-stu-id="93a57-442">Instead, the `foreach` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="93a57-443">Par exemple, le code suivant retourne les éléments dans le `Request.ServerVariables` collection, qui est un objet qui contient des informations sur votre serveur web.</span><span class="sxs-lookup"><span data-stu-id="93a57-443">For example, the following code returns the items in the `Request.ServerVariables` collection, which is an object that contains information about your web server.</span></span> <span data-ttu-id="93a57-444">Il utilise un `foreac` h boucle pour afficher le nom de chaque élément en créant un `<li>` élément dans une liste à puces HTML.</span><span class="sxs-lookup"><span data-stu-id="93a57-444">It uses a `foreac` h loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample54.html)]

<span data-ttu-id="93a57-445">Le `foreach` mot clé est suivi par des parenthèses lorsque vous déclarez une variable qui représente un élément unique dans la collection (dans l’exemple, `var item`), suivi par le `in` (mot clé), suivie de la collection que vous souhaitez parcourir.</span><span class="sxs-lookup"><span data-stu-id="93a57-445">The `foreach` keyword is followed by parentheses where you declare a variable that represents a single item in the collection (in the example, `var item`), followed by the `in` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="93a57-446">Dans le corps de la `foreach` boucle, vous pouvez accéder à l’élément actuel à l’aide de la variable que vous avez déclaré précédemment.</span><span class="sxs-lookup"><span data-stu-id="93a57-446">In the body of the `foreach` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-c/_static/image12.jpg)

<span data-ttu-id="93a57-448">Pour créer une boucle plus à usage général, utilisez la `while` instruction :</span><span class="sxs-lookup"><span data-stu-id="93a57-448">To create a more general-purpose loop, use the `while` statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample55.cshtml)]

<span data-ttu-id="93a57-449">A `while` boucle commence par la `while` (mot clé), suivi de parenthèses dans lequel vous spécifiez la durée pendant laquelle la boucle continue (ici, pour tant que `countNum` est inférieure à 50), puis le bloc à répéter.</span><span class="sxs-lookup"><span data-stu-id="93a57-449">A `while` loop begins with the `while` keyword, followed by parentheses where you specify how long the loop continues (here, for as long as `countNum` is less than 50), then the block to repeat.</span></span> <span data-ttu-id="93a57-450">Boucles incrémentent généralement (ajouter à) ou de décrémentation (Soustraire) une variable ou un objet utilisé pour l’inventaire.</span><span class="sxs-lookup"><span data-stu-id="93a57-450">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="93a57-451">Dans l’exemple, le `+=` opérateur ajoute 1 à `countNum` chaque fois que la boucle s’exécute.</span><span class="sxs-lookup"><span data-stu-id="93a57-451">In the example, the `+=` operator adds 1 to `countNum` each time the loop runs.</span></span> <span data-ttu-id="93a57-452">(Pour décrémenter une variable dans une boucle qui compte vers le bas, vous utiliseriez l’opérateur de décrémentation `-=`).</span><span class="sxs-lookup"><span data-stu-id="93a57-452">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`).</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="93a57-453">Objets et Collections</span><span class="sxs-lookup"><span data-stu-id="93a57-453">Objects and Collections</span></span>

<span data-ttu-id="93a57-454">Quasiment tout dans un site Web ASP.NET est un objet, y compris la page web.</span><span class="sxs-lookup"><span data-stu-id="93a57-454">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="93a57-455">Cette section décrit certains objets importants que vous utiliserez fréquemment dans votre code.</span><span class="sxs-lookup"><span data-stu-id="93a57-455">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="93a57-456">Objets de la page</span><span class="sxs-lookup"><span data-stu-id="93a57-456">Page Objects</span></span>

<span data-ttu-id="93a57-457">L’objet de base dans ASP.NET est la page.</span><span class="sxs-lookup"><span data-stu-id="93a57-457">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="93a57-458">Vous pouvez accéder aux propriétés de l’objet page directement sans aucun objet éligible.</span><span class="sxs-lookup"><span data-stu-id="93a57-458">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="93a57-459">Le code suivant obtient le chemin d’accès du fichier de la page, à l’aide de la `Request` objet de la page :</span><span class="sxs-lookup"><span data-stu-id="93a57-459">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample56.cshtml)]

<span data-ttu-id="93a57-460">Pour le rendre clair que que vous référencez les propriétés et méthodes de l’objet en cours de la page, vous pouvez éventuellement utiliser le mot clé `this` pour représenter l’objet de la page dans votre code.</span><span class="sxs-lookup"><span data-stu-id="93a57-460">To make it clear that you're referencing properties and methods on the current page object, you can optionally use the keyword `this` to represent the page object in your code.</span></span> <span data-ttu-id="93a57-461">Voici l’exemple de code précédent, avec `this` ajoutée pour représenter la page :</span><span class="sxs-lookup"><span data-stu-id="93a57-461">Here is the previous code example, with `this` added to represent the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample57.cshtml)]

<span data-ttu-id="93a57-462">Vous pouvez utiliser les propriétés de la `Page` objet pour obtenir un grand nombre d’informations, telles que :</span><span class="sxs-lookup"><span data-stu-id="93a57-462">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="93a57-463">`Request`.</span><span class="sxs-lookup"><span data-stu-id="93a57-463">`Request`.</span></span> <span data-ttu-id="93a57-464">Comme vous l’avez déjà vu, il s’agit d’une collection d’informations sur la demande en cours, y compris le type de navigateur fait la demande, l’URL de la page, de l’identité de l’utilisateur, etc.</span><span class="sxs-lookup"><span data-stu-id="93a57-464">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="93a57-465">`Response`.</span><span class="sxs-lookup"><span data-stu-id="93a57-465">`Response`.</span></span> <span data-ttu-id="93a57-466">Il s’agit d’une collection d’informations sur la réponse qui sera envoyée au navigateur lorsque l’exécution est terminée dans le code du serveur (page).</span><span class="sxs-lookup"><span data-stu-id="93a57-466">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="93a57-467">Par exemple, vous pouvez utiliser cette propriété pour écrire des informations dans la réponse.</span><span class="sxs-lookup"><span data-stu-id="93a57-467">For example, you can use this property to write information into the response.</span></span> 

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample58.cshtml)]

<a id="ID_CollectionsAndObjects"></a>
### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="93a57-468">Objets de collection (tableaux et les dictionnaires)</span><span class="sxs-lookup"><span data-stu-id="93a57-468">Collection Objects (Arrays and Dictionaries)</span></span>

<span data-ttu-id="93a57-469">A *collection* est un groupe d’objets du même type, comme une collection de `Customer` objets à partir d’une base de données.</span><span class="sxs-lookup"><span data-stu-id="93a57-469">A *collection* is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="93a57-470">ASP.NET contient de nombreuses collections intégrées, comme le `Request.Files` collection.</span><span class="sxs-lookup"><span data-stu-id="93a57-470">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="93a57-471">Souvent, vous allez travailler avec des données dans des collections.</span><span class="sxs-lookup"><span data-stu-id="93a57-471">You'll often work with data in collections.</span></span> <span data-ttu-id="93a57-472">Deux types de collection courantes sont les *tableau* et *dictionnaire*.</span><span class="sxs-lookup"><span data-stu-id="93a57-472">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="93a57-473">Un tableau est utile lorsque vous souhaitez stocker une collection d’éléments similaires mais que vous ne souhaitez pas créer une variable distincte pour chaque élément :</span><span class="sxs-lookup"><span data-stu-id="93a57-473">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample59.cshtml)]

<span data-ttu-id="93a57-474">Les tableaux, vous déclarez un type de données spécifique, tel que `string`, `int`, ou `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="93a57-474">With arrays, you declare a specific data type, such as `string`, `int`, or `DateTime`.</span></span> <span data-ttu-id="93a57-475">Pour indiquer que la variable peut contenir un tableau, vous ajoutez des crochets à la déclaration (tel que `string[]` ou `int[]`).</span><span class="sxs-lookup"><span data-stu-id="93a57-475">To indicate that the variable can contain an array, you add brackets to the declaration (such as `string[]` or `int[]`).</span></span> <span data-ttu-id="93a57-476">Vous pouvez accéder à des éléments dans un tableau à l’aide de leur position (index) ou en utilisant la `foreach` instruction.</span><span class="sxs-lookup"><span data-stu-id="93a57-476">You can access items in an array using their position (index) or by using the `foreach` statement.</span></span> <span data-ttu-id="93a57-477">Tableau d’index sont de base zéro &#8212; Autrement dit, le premier élément est à position 0, le deuxième élément correspond à la position 1 et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="93a57-477">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample60.cshtml)]

<span data-ttu-id="93a57-478">Vous pouvez déterminer le nombre d’éléments dans un tableau en obtenant son `Length` propriété.</span><span class="sxs-lookup"><span data-stu-id="93a57-478">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="93a57-479">Pour obtenir la position d’un élément spécifique dans le tableau (à la recherche dans le tableau), utilisez le `Array.IndexOf` (méthode).</span><span class="sxs-lookup"><span data-stu-id="93a57-479">To get the position of a specific item in the array (to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="93a57-480">Vous pouvez également effectuer les opérations inverse le contenu d’un tableau (le `Array.Reverse` méthode) ou de trier le contenu (la `Array.Sort` méthode).</span><span class="sxs-lookup"><span data-stu-id="93a57-480">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="93a57-481">La sortie du code de tableau de chaîne affichée dans un navigateur :</span><span class="sxs-lookup"><span data-stu-id="93a57-481">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-c/_static/image13.jpg)

<span data-ttu-id="93a57-483">Un dictionnaire est une collection de paires clé/valeur, où vous fournissez la clé (ou nom) pour définir ou récupérer la valeur correspondante :</span><span class="sxs-lookup"><span data-stu-id="93a57-483">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample61.cshtml)]

<span data-ttu-id="93a57-484">Pour créer un dictionnaire, vous utilisez la `new` (mot clé) pour indiquer que vous créez un nouvel objet de dictionnaire.</span><span class="sxs-lookup"><span data-stu-id="93a57-484">To create a dictionary, you use the `new` keyword to indicate that you're creating a new dictionary object.</span></span> <span data-ttu-id="93a57-485">Vous pouvez affecter un dictionnaire à une variable à l’aide du `var` (mot clé).</span><span class="sxs-lookup"><span data-stu-id="93a57-485">You can assign a dictionary to a variable using the `var` keyword.</span></span> <span data-ttu-id="93a57-486">Vous indiquez les types de données des éléments dans le dictionnaire à l’aide de crochets pointus ( `< >` ).</span><span class="sxs-lookup"><span data-stu-id="93a57-486">You indicate the data types of the items in the dictionary using angle brackets ( `< >` ).</span></span> <span data-ttu-id="93a57-487">À la fin de la déclaration, vous devez ajouter une paire de parenthèses, car il s’agit en fait une méthode qui crée un dictionnaire.</span><span class="sxs-lookup"><span data-stu-id="93a57-487">At the end of the declaration, you must add a pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="93a57-488">Pour ajouter des éléments au dictionnaire, vous pouvez appeler la `Add` méthode de la variable de dictionnaire (`myScores` dans ce cas), puis spécifiez une clé et une valeur.</span><span class="sxs-lookup"><span data-stu-id="93a57-488">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="93a57-489">Ou bien, vous pouvez utiliser des crochets pour indiquer la clé et effectuez une assignation simple, comme dans l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="93a57-489">Alternatively, you can use square brackets to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample62.cs)]

<span data-ttu-id="93a57-490">Pour obtenir une valeur à partir du dictionnaire, vous spécifiez la clé entre crochets :</span><span class="sxs-lookup"><span data-stu-id="93a57-490">To get a value from the dictionary, you specify the key in brackets:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample63.cs)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="93a57-491">Appel de méthodes avec des paramètres</span><span class="sxs-lookup"><span data-stu-id="93a57-491">Calling Methods with Parameters</span></span>

<span data-ttu-id="93a57-492">Lorsque vous lisez plus haut dans cet article, les objets que vous programmez avec peuvent avoir des méthodes.</span><span class="sxs-lookup"><span data-stu-id="93a57-492">As you read earlier in this article, the objects that you program with can have methods.</span></span> <span data-ttu-id="93a57-493">Par exemple, un `Database` objet peut avoir un `Database.Connect` (méthode).</span><span class="sxs-lookup"><span data-stu-id="93a57-493">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="93a57-494">De nombreuses méthodes ont également un ou plusieurs paramètres.</span><span class="sxs-lookup"><span data-stu-id="93a57-494">Many methods also have one or more parameters.</span></span> <span data-ttu-id="93a57-495">A *paramètre* est une valeur que vous passez à une méthode pour activer la méthode effectuer sa tâche.</span><span class="sxs-lookup"><span data-stu-id="93a57-495">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="93a57-496">Par exemple, examinez une déclaration pour le `Request.MapPath` méthode qui accepte trois paramètres :</span><span class="sxs-lookup"><span data-stu-id="93a57-496">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample64.cs)]

<span data-ttu-id="93a57-497">(La ligne a été encapsulée pour le rendre plus lisible.</span><span class="sxs-lookup"><span data-stu-id="93a57-497">(The line has been wrapped to make it more readable.</span></span> <span data-ttu-id="93a57-498">Souvenez-vous que vous pouvez placer des sauts de ligne presque partout sauf à l’intérieur des chaînes qui sont placés entre guillemets.)</span><span class="sxs-lookup"><span data-stu-id="93a57-498">Remember that you can put line breaks almost any place except inside strings that are enclosed in quotation marks.)</span></span>

<span data-ttu-id="93a57-499">Cette méthode retourne le chemin d’accès physique sur le serveur qui correspond à un chemin d’accès virtuel spécifié.</span><span class="sxs-lookup"><span data-stu-id="93a57-499">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="93a57-500">Les trois paramètres pour la méthode sont `virtualPath`, `baseVirtualDir`, et `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="93a57-500">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="93a57-501">(Notez que dans la déclaration, les paramètres sont répertoriés avec les types de données des données qu’ils vous acceptez). Lorsque vous appelez cette méthode, vous devez fournir des valeurs pour tous les trois paramètres.</span><span class="sxs-lookup"><span data-stu-id="93a57-501">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="93a57-502">La syntaxe Razor offre deux options pour passer des paramètres à une méthode : *paramètres positionnels* et *des paramètres nommés*.</span><span class="sxs-lookup"><span data-stu-id="93a57-502">The Razor syntax gives you two options for passing parameters to a method: *positional parameters* and *named parameters*.</span></span> <span data-ttu-id="93a57-503">Pour appeler une méthode à l’aide de paramètres positionnels, vous transmettez les paramètres dans un ordre strict qui est spécifié dans la déclaration de méthode.</span><span class="sxs-lookup"><span data-stu-id="93a57-503">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="93a57-504">(Vous généralement sauriez cet ordre en lisant la documentation de la méthode.) Vous devez suivre l’ordre, et vous ne pouvez pas ignorer les paramètres &#8212; Si nécessaire, vous passez une chaîne vide (`""`) ou `null` pour un paramètre positionnel que vous n’avez pas une valeur pour.</span><span class="sxs-lookup"><span data-stu-id="93a57-504">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or `null` for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="93a57-505">L’exemple suivant suppose que vous avez un dossier nommé *scripts* sur votre site Web.</span><span class="sxs-lookup"><span data-stu-id="93a57-505">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="93a57-506">Le code appelle la `Request.MapPath` méthode et passe les valeurs pour les trois paramètres dans l’ordre approprié.</span><span class="sxs-lookup"><span data-stu-id="93a57-506">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="93a57-507">Il affiche ensuite le chemin d’accès résultant mappé.</span><span class="sxs-lookup"><span data-stu-id="93a57-507">It then displays the resulting mapped path.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample65.cshtml)]

<span data-ttu-id="93a57-508">Lorsqu’une méthode possède de nombreux paramètres, vous pouvez conserver votre code plus lisible à l’aide des paramètres nommés.</span><span class="sxs-lookup"><span data-stu-id="93a57-508">When a method has many parameters, you can keep your code more readable by using named parameters.</span></span> <span data-ttu-id="93a57-509">Pour appeler une méthode à l’aide de paramètres nommés, vous spécifiez le nom du paramètre suivi par un signe deux-points ( :), puis la valeur.</span><span class="sxs-lookup"><span data-stu-id="93a57-509">To call a method using named parameters, you specify the parameter name followed by a colon (:), and then the value.</span></span> <span data-ttu-id="93a57-510">L’avantage de paramètres nommés, que vous pouvez les passer dans l’ordre souhaité.</span><span class="sxs-lookup"><span data-stu-id="93a57-510">The advantage of named parameters is that you can pass them in any order you want.</span></span> <span data-ttu-id="93a57-511">(Un inconvénient est que l’appel de méthode n’est pas aussi compact).</span><span class="sxs-lookup"><span data-stu-id="93a57-511">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="93a57-512">L’exemple suivant appelle la même méthode que ci-dessus, mais utilise des paramètres nommés pour fournir les valeurs :</span><span class="sxs-lookup"><span data-stu-id="93a57-512">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample66.cshtml)]

<span data-ttu-id="93a57-513">Comme vous pouvez le voir, les paramètres sont passés dans un ordre différent.</span><span class="sxs-lookup"><span data-stu-id="93a57-513">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="93a57-514">Cependant, si vous exécutez l’exemple précédent et cet exemple, ils allez retournent la même valeur.</span><span class="sxs-lookup"><span data-stu-id="93a57-514">However, if you run the previous example and this example, they'll return the same value.</span></span>

<a id="ID_HandlingErrors"></a>
## <a name="handling-errors"></a><span data-ttu-id="93a57-515">Gestion des erreurs</span><span class="sxs-lookup"><span data-stu-id="93a57-515">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="93a57-516">Instructions Try-Catch</span><span class="sxs-lookup"><span data-stu-id="93a57-516">Try-Catch Statements</span></span>

<span data-ttu-id="93a57-517">Vous aurez souvent des instructions dans votre code peut échouer pour des raisons en dehors de votre contrôle.</span><span class="sxs-lookup"><span data-stu-id="93a57-517">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="93a57-518">Exemple :</span><span class="sxs-lookup"><span data-stu-id="93a57-518">For example:</span></span>

- <span data-ttu-id="93a57-519">Si votre code essaie de créer ou d’accéder à un fichier, toutes sortes d’erreurs peuvent se produire.</span><span class="sxs-lookup"><span data-stu-id="93a57-519">If your code tries to create or access a file, all sorts of errors might occur.</span></span> <span data-ttu-id="93a57-520">Le fichier n’existe ne peut-être pas, il est peut-être verrouillé, le code ne peut pas disposer des autorisations et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="93a57-520">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="93a57-521">De même, si votre code essaie de mettre à jour des enregistrements dans une base de données, il peut y avoir des problèmes d’autorisations, la connexion à la base de données peut-être être supprimée, les données à enregistrer est peut-être non valide et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="93a57-521">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="93a57-522">En termes de programmation, ces situations sont appelées *exceptions*.</span><span class="sxs-lookup"><span data-stu-id="93a57-522">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="93a57-523">Si votre code rencontre une exception, il génère (lève) un message d’erreur de, au mieux, agaçantes aux utilisateurs :</span><span class="sxs-lookup"><span data-stu-id="93a57-523">If your code encounters an exception, it generates (throws) an error message that's, at best, annoying to users:</span></span>

![Razor-Img14](introducing-razor-syntax-c/_static/image14.jpg)

<span data-ttu-id="93a57-525">Dans les situations où votre code peut rencontrer des exceptions et afin d’éviter les messages d’erreur de ce type, vous pouvez utiliser `try/catch` instructions.</span><span class="sxs-lookup"><span data-stu-id="93a57-525">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `try/catch` statements.</span></span> <span data-ttu-id="93a57-526">Dans la `try` instruction, vous exécutez le code que vous êtes en train de.</span><span class="sxs-lookup"><span data-stu-id="93a57-526">In the `try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="93a57-527">Dans un ou plusieurs `catch` instructions, vous pouvez rechercher propres erreurs (types spécifiques d’exceptions) qui se sont produites.</span><span class="sxs-lookup"><span data-stu-id="93a57-527">In one or more `catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="93a57-528">Vous pouvez inclure autant `catch` instructions que vous avez besoin rechercher des erreurs que vous comptez.</span><span class="sxs-lookup"><span data-stu-id="93a57-528">You can include as many `catch` statements as you need to look for errors that you are anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="93a57-529">Nous vous recommandons d’éviter à l’aide de la `Response.Redirect` méthode dans `try/catch` instructions, car il peut provoquer une exception dans votre page.</span><span class="sxs-lookup"><span data-stu-id="93a57-529">We recommend that you avoid using the `Response.Redirect` method in `try/catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="93a57-530">L’exemple suivant montre une page qui crée un fichier texte sur la première requête, puis affiche un bouton qui permet à l’utilisateur d’ouvrir le fichier.</span><span class="sxs-lookup"><span data-stu-id="93a57-530">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="93a57-531">L’exemple utilise délibérément un nom de fichier incorrect afin qu’il génère une exception.</span><span class="sxs-lookup"><span data-stu-id="93a57-531">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="93a57-532">Le code inclut `catch` instructions pour les deux exceptions possibles : `FileNotFoundException`, qui se produit si le nom de fichier est incorrect, et `DirectoryNotFoundException`, qui se produit si ASP.NET même Impossible de trouver le dossier.</span><span class="sxs-lookup"><span data-stu-id="93a57-532">The code includes `catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="93a57-533">(Vous pouvez ne pas commenter une instruction dans l’exemple pour voir comment il s’exécute lorsque tout fonctionne correctement.)</span><span class="sxs-lookup"><span data-stu-id="93a57-533">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="93a57-534">Si votre code n’a pas gérer l’exception, vous voyez une page d’erreur comme la capture d’écran précédente.</span><span class="sxs-lookup"><span data-stu-id="93a57-534">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="93a57-535">Toutefois, le `try/catch` section vous aide à empêcher l’utilisateur de voir ces types d’erreurs.</span><span class="sxs-lookup"><span data-stu-id="93a57-535">However, the `try/catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample67.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="93a57-536">Ressources supplémentaires</span><span class="sxs-lookup"><span data-stu-id="93a57-536">Additional Resources</span></span>

<span data-ttu-id="93a57-537">**Programmation avec Visual Basic**</span><span class="sxs-lookup"><span data-stu-id="93a57-537">**Programming with Visual Basic**</span></span>


[<span data-ttu-id="93a57-538">Annexe : langage Visual Basic et syntaxe</span><span class="sxs-lookup"><span data-stu-id="93a57-538">Appendix: Visual Basic Language and Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkId=202908)


<span data-ttu-id="93a57-539">**Documentation de référence**</span><span class="sxs-lookup"><span data-stu-id="93a57-539">**Reference Documentation**</span></span>


[<span data-ttu-id="93a57-540">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="93a57-540">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)

[<span data-ttu-id="93a57-541">Langage c#</span><span class="sxs-lookup"><span data-stu-id="93a57-541">C# Language</span></span>](https://msdn.microsoft.com/library/kx37x362.aspx)
