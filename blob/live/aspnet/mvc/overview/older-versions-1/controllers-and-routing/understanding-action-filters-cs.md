---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
title: "Présentation des filtres d’Action (c#) | Documents Microsoft"
author: microsoft
description: "L’objectif de ce didacticiel est d’expliquer les filtres d’action. Un filtre d’action est un attribut que vous pouvez appliquer à une action de contrôleur--ou à un contrôleur ensemble..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 10/16/2008
ms.topic: article
ms.assetid: a94e4e81-40c1-47b7-8613-126a1a6cc93d
ms.technology: dotnet-mvc
ms.prod: .net-framework
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
msc.type: authoredcontent
ms.openlocfilehash: 1f469894022e39048154ec1915237e448104b4b6
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 11/10/2017
---
<a name="understanding-action-filters-c"></a><span data-ttu-id="a94ea-104">Présentation des filtres d’Action (c#)</span><span class="sxs-lookup"><span data-stu-id="a94ea-104">Understanding Action Filters (C#)</span></span>
====================
<span data-ttu-id="a94ea-105">par [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="a94ea-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="a94ea-106">Télécharger le PDF</span><span class="sxs-lookup"><span data-stu-id="a94ea-106">Download PDF</span></span>](http://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_CS.pdf)

> <span data-ttu-id="a94ea-107">L’objectif de ce didacticiel est d’expliquer les filtres d’action.</span><span class="sxs-lookup"><span data-stu-id="a94ea-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="a94ea-108">Un filtre d’action est un attribut que vous pouvez appliquer à une action de contrôleur--ou à un contrôleur entière--qui modifie la façon dont dans lequel l’action est exécutée.</span><span class="sxs-lookup"><span data-stu-id="a94ea-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>


## <a name="understanding-action-filters"></a><span data-ttu-id="a94ea-109">Présentation des filtres d’Action</span><span class="sxs-lookup"><span data-stu-id="a94ea-109">Understanding Action Filters</span></span>

<span data-ttu-id="a94ea-110">L’objectif de ce didacticiel est d’expliquer les filtres d’action.</span><span class="sxs-lookup"><span data-stu-id="a94ea-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="a94ea-111">Un filtre d’action est un attribut que vous pouvez appliquer à une action de contrôleur--ou à un contrôleur entière--qui modifie la façon dont dans lequel l’action est exécutée.</span><span class="sxs-lookup"><span data-stu-id="a94ea-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="a94ea-112">L’infrastructure ASP.NET MVC inclut plusieurs filtres d’action :</span><span class="sxs-lookup"><span data-stu-id="a94ea-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="a94ea-113">OutputCache – ce filtre d’action met en cache la sortie d’une action de contrôleur pour une durée spécifiée.</span><span class="sxs-lookup"><span data-stu-id="a94ea-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="a94ea-114">HandleError – ce filtre d’action gère les erreurs qui se produisent lors de l’exécution d’une action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="a94ea-115">Autoriser – ce filtre d’action vous permet de restreindre l’accès à un utilisateur particulier ou un rôle.</span><span class="sxs-lookup"><span data-stu-id="a94ea-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="a94ea-116">Vous pouvez également créer vos propres filtres d’action personnalisés.</span><span class="sxs-lookup"><span data-stu-id="a94ea-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="a94ea-117">Par exemple, vous souhaiterez créer un filtre d’action personnalisé pour implémenter un système d’authentification personnalisé.</span><span class="sxs-lookup"><span data-stu-id="a94ea-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="a94ea-118">Ou bien, vous souhaiterez peut-être créer un filtre d’action qui modifie les données retournées par une action de contrôleur d’affichage.</span><span class="sxs-lookup"><span data-stu-id="a94ea-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="a94ea-119">Dans ce didacticiel, vous allez apprendre à créer un filtre d’action de toutes pièces.</span><span class="sxs-lookup"><span data-stu-id="a94ea-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="a94ea-120">Nous créons un filtre d’action de journal qui consigne les différentes étapes du traitement d’une action dans la fenêtre de sortie de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a94ea-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="a94ea-121">À l’aide d’un filtre d’Action</span><span class="sxs-lookup"><span data-stu-id="a94ea-121">Using an Action Filter</span></span>

<span data-ttu-id="a94ea-122">Un filtre d’action est un attribut.</span><span class="sxs-lookup"><span data-stu-id="a94ea-122">An action filter is an attribute.</span></span> <span data-ttu-id="a94ea-123">Vous pouvez appliquer la plupart des filtres d’action pour une action de contrôleur individuels ou d’un contrôleur entière.</span><span class="sxs-lookup"><span data-stu-id="a94ea-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="a94ea-124">Par exemple, le contrôleur de données dans la liste 1 expose une action nommée `Index()` qui retourne l’heure actuelle.</span><span class="sxs-lookup"><span data-stu-id="a94ea-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="a94ea-125">Cette action est décorée avec le `OutputCache` filtre d’action.</span><span class="sxs-lookup"><span data-stu-id="a94ea-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="a94ea-126">Ce filtre provoque la valeur retournée par l’action doit être mis en cache pendant 10 secondes.</span><span class="sxs-lookup"><span data-stu-id="a94ea-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="a94ea-127">**La liste 1 :`Controllers\DataController.cs`**</span><span class="sxs-lookup"><span data-stu-id="a94ea-127">**Listing 1 – `Controllers\DataController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample1.cs)]

<span data-ttu-id="a94ea-128">Si vous appelez à plusieurs reprises la `Index()` action en entrant l’URL/données/Index dans la barre d’adresses de votre navigateur et en appuyant sur l’actualisation bouton plusieurs fois, vous verrez simultanément pendant 10 secondes.</span><span class="sxs-lookup"><span data-stu-id="a94ea-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="a94ea-129">La sortie de la `Index()` action est mis en cache pendant 10 secondes (voir Figure 1).</span><span class="sxs-lookup"><span data-stu-id="a94ea-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>


<span data-ttu-id="a94ea-130">[![Fois mis en cache](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="a94ea-130">[![Cached time](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span></span>

<span data-ttu-id="a94ea-131">**Figure 01**: heure de mise en cache ([cliquez pour afficher l’image en taille réelle](understanding-action-filters-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="a94ea-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-cs/_static/image3.png))</span></span>


<span data-ttu-id="a94ea-132">Dans la liste 1, un filtre d’action unique : le `OutputCache` filtre d’action : est appliqué à la `Index()` (méthode).</span><span class="sxs-lookup"><span data-stu-id="a94ea-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="a94ea-133">Si vous avez besoin, vous pouvez appliquer plusieurs filtres d’action pour la même action.</span><span class="sxs-lookup"><span data-stu-id="a94ea-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="a94ea-134">Par exemple, vous pouvez souhaiter appliquer à la fois le `OutputCache` et `HandleError` filtres d’action pour la même action.</span><span class="sxs-lookup"><span data-stu-id="a94ea-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="a94ea-135">Dans la liste de 1, le `OutputCache` filtre d’action est appliquée à la `Index()` action.</span><span class="sxs-lookup"><span data-stu-id="a94ea-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="a94ea-136">Vous pouvez également appliquer cet attribut à la `DataController` classe elle-même.</span><span class="sxs-lookup"><span data-stu-id="a94ea-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="a94ea-137">Dans ce cas, le résultat retourné par une action exposée par le contrôleur est mis en cache pendant 10 secondes.</span><span class="sxs-lookup"><span data-stu-id="a94ea-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="a94ea-138">Les différents Types de filtres</span><span class="sxs-lookup"><span data-stu-id="a94ea-138">The Different Types of Filters</span></span>

<span data-ttu-id="a94ea-139">L’infrastructure ASP.NET MVC prend en charge quatre types de filtres :</span><span class="sxs-lookup"><span data-stu-id="a94ea-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="a94ea-140">Filtres d’autorisation : implémente le `IAuthorizationFilter` attribut.</span><span class="sxs-lookup"><span data-stu-id="a94ea-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="a94ea-141">Filtres d’action : implémente le `IActionFilter` attribut.</span><span class="sxs-lookup"><span data-stu-id="a94ea-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="a94ea-142">Générer des filtres : implémente le `IResultFilter` attribut.</span><span class="sxs-lookup"><span data-stu-id="a94ea-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="a94ea-143">Filtres d’exception : implémente le `IExceptionFilter` attribut.</span><span class="sxs-lookup"><span data-stu-id="a94ea-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="a94ea-144">Les filtres sont exécutés dans l’ordre indiqué ci-dessus.</span><span class="sxs-lookup"><span data-stu-id="a94ea-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="a94ea-145">Par exemple, les filtres d’autorisation sont toujours exécutés avant les filtres d’action et les filtres d’exception sont toujours exécutées après tous les autres types de filtre.</span><span class="sxs-lookup"><span data-stu-id="a94ea-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="a94ea-146">Filtres d’autorisation sont utilisées pour implémenter l’authentification et autorisation pour les actions de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="a94ea-147">Par exemple, le filtre Authorize est un exemple de filtre d’autorisation.</span><span class="sxs-lookup"><span data-stu-id="a94ea-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="a94ea-148">Filtres d’action contiennent une logique qui est exécutée avant et après l’exécution d’une action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="a94ea-149">Vous pouvez utiliser un filtre d’action, par exemple, pour modifier les données d’affichage renvoyant à une action du contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="a94ea-150">Filtres de résultat contiennent une logique qui est exécutée avant et après l’exécution du résultat d’une vue.</span><span class="sxs-lookup"><span data-stu-id="a94ea-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="a94ea-151">Par exemple, vous pouvez souhaiter modifier un résultat de la vue avant que la vue est restituée dans le navigateur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="a94ea-152">Filtres d’exception sont le dernier type de filtre à exécuter.</span><span class="sxs-lookup"><span data-stu-id="a94ea-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="a94ea-153">Vous pouvez utiliser un filtre d’exception à gérer les erreurs déclenchées par vos actions de contrôleur ou résultats d’action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="a94ea-154">Vous pouvez également utiliser les filtres d’exception pour consigner les erreurs.</span><span class="sxs-lookup"><span data-stu-id="a94ea-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="a94ea-155">Chaque type de filtre est exécutée dans un ordre particulier.</span><span class="sxs-lookup"><span data-stu-id="a94ea-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="a94ea-156">Si vous souhaitez contrôler l’ordre dans lequel les filtres du même type sont exécutées vous pouvez définir les propriétés de commande d’un filtre.</span><span class="sxs-lookup"><span data-stu-id="a94ea-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="a94ea-157">La classe de base pour tous les filtres d’action est la `System.Web.Mvc.FilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="a94ea-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="a94ea-158">Si vous souhaitez implémenter un type particulier de filtre, vous devez créer une classe qui hérite de la classe de filtre de base et implémente une ou plusieurs de la `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, ou `ExceptionFilter` interfaces.</span><span class="sxs-lookup"><span data-stu-id="a94ea-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, or `ExceptionFilter` interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="a94ea-159">La classe de Base ActionFilterAttribute</span><span class="sxs-lookup"><span data-stu-id="a94ea-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="a94ea-160">Pour le rendre plus facile à implémenter un filtre d’action personnalisé, l’infrastructure ASP.NET MVC inclut une base de `ActionFilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="a94ea-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="a94ea-161">Cette classe implémente à la fois le `IActionFilter` et `IResultFilter` interfaces et hérite de la `Filter` classe.</span><span class="sxs-lookup"><span data-stu-id="a94ea-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="a94ea-162">La terminologie ici n’est pas entièrement cohérente.</span><span class="sxs-lookup"><span data-stu-id="a94ea-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="a94ea-163">Techniquement, une classe qui hérite de la classe ActionFilterAttribute est un filtre d’action et un filtre de résultat.</span><span class="sxs-lookup"><span data-stu-id="a94ea-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="a94ea-164">Toutefois, dans le sens de pertes de connexion, le filtre d’action word est utilisé pour faire référence à n’importe quel type de filtre dans l’infrastructure ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="a94ea-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="a94ea-165">La base de `ActionFilterAttribute` classe a les méthodes suivantes que vous pouvez substituer :</span><span class="sxs-lookup"><span data-stu-id="a94ea-165">The base `ActionFilterAttribute` class has the following methods that you can override:</span></span>

- <span data-ttu-id="a94ea-166">OnActionExecuting – cette méthode est appelée avant l’exécution d’une action du contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="a94ea-167">OnActionExecuted – cette méthode est appelée après l’exécution d’une action du contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="a94ea-168">OnResultExecuting : cette méthode est appelée avant l’exécution d’un résultat d’action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="a94ea-169">OnResultExecuted – cette méthode est appelée après l’exécution d’un résultat d’action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="a94ea-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="a94ea-170">Dans la section suivante, nous verrons comment vous pouvez implémenter chacun de ces différentes méthodes.</span><span class="sxs-lookup"><span data-stu-id="a94ea-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="a94ea-171">Création d’un filtre d’Action de journal</span><span class="sxs-lookup"><span data-stu-id="a94ea-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="a94ea-172">Afin d’illustrer comment vous pouvez créer un filtre d’action personnalisé, nous allons créer un filtre d’action personnalisé qui enregistre les étapes de traitement d’une action du contrôleur dans la fenêtre Sortie de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a94ea-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="a94ea-173">Notre `LogActionFilter` est contenue dans la liste 2.</span><span class="sxs-lookup"><span data-stu-id="a94ea-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="a94ea-174">**Liste 2 :`ActionFilters\LogActionFilter.cs`**</span><span class="sxs-lookup"><span data-stu-id="a94ea-174">**Listing 2 – `ActionFilters\LogActionFilter.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample2.cs)]

<span data-ttu-id="a94ea-175">Dans la liste 2, le `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, et `OnResultExecuted()` toutes les méthodes appellent le `Log()` (méthode).</span><span class="sxs-lookup"><span data-stu-id="a94ea-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="a94ea-176">Le nom de la méthode et les données d’itinéraire actuel est passé à la `Log()` (méthode).</span><span class="sxs-lookup"><span data-stu-id="a94ea-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="a94ea-177">Le `Log()` méthode écrit un message dans la fenêtre Sortie de Visual Studio (voir Figure 2).</span><span class="sxs-lookup"><span data-stu-id="a94ea-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>


<span data-ttu-id="a94ea-178">[![Écriture dans la fenêtre Sortie de Visual Studio](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="a94ea-178">[![Writing to the Visual Studio Output window](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span></span>

<span data-ttu-id="a94ea-179">**Figure 02**: l’écriture dans la fenêtre Sortie de Visual Studio ([cliquez pour afficher l’image en taille réelle](understanding-action-filters-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="a94ea-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-cs/_static/image6.png))</span></span>


<span data-ttu-id="a94ea-180">Le contrôleur Home dans le Listing 3 illustre comment vous pouvez appliquer le filtre d’action de journal à une classe de contrôleur entière.</span><span class="sxs-lookup"><span data-stu-id="a94ea-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="a94ea-181">Chaque fois que toutes les actions exposées par le contrôleur Home sont appelées – soit le `Index()` méthode ou la `About()` méthode – les étapes de traitement de l’action sont enregistrés dans la fenêtre Sortie de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a94ea-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="a94ea-182">**La liste 3 :`Controllers\HomeController.cs`**</span><span class="sxs-lookup"><span data-stu-id="a94ea-182">**Listing 3 – `Controllers\HomeController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample3.cs)]

### <a name="summary"></a><span data-ttu-id="a94ea-183">Résumé</span><span class="sxs-lookup"><span data-stu-id="a94ea-183">Summary</span></span>

<span data-ttu-id="a94ea-184">Dans ce didacticiel, vous ont été introduits pour les filtres d’action ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="a94ea-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="a94ea-185">Vous avez appris les quatre différents types de filtres : filtres d’autorisation, les filtres d’action, les filtres de résultat et les filtres d’exception.</span><span class="sxs-lookup"><span data-stu-id="a94ea-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="a94ea-186">Vous avez également appris sur la base `ActionFilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="a94ea-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="a94ea-187">Enfin, vous avez appris comment implémenter un filtre d’action simple.</span><span class="sxs-lookup"><span data-stu-id="a94ea-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="a94ea-188">Nous avons créé un filtre d’action de journal qui enregistre les étapes de traitement d’une action du contrôleur dans la fenêtre Sortie de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a94ea-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="a94ea-189">[Précédent](asp-net-mvc-routing-overview-cs.md)
[Suivant](improving-performance-with-output-caching-cs.md)</span><span class="sxs-lookup"><span data-stu-id="a94ea-189">[Previous](asp-net-mvc-routing-overview-cs.md)
[Next](improving-performance-with-output-caching-cs.md)</span></span>
